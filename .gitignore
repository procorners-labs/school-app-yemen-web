# مجلدات الكود المصدري — تُدار في school-app-yemen-gas (الخاص)
/teacher/
/student/
/home/
/cms/
/schedule/
/master-admin/
_build/
docs/
package.json
دليل-التشغيل.md

# مجلدات Claude Code المحلية
.claude/

# ملفات Windows
Thumbs.db
desktop.ini

# node_modules
node_modules/

# ملفات بيئة
.env
*.local

# كاش wrangler المحلّي — يحوي معرّف حساب Cloudflare واسمه (بريد المالك).
# 🔴 هذا المستودع **عام** (وبقاؤه عاماً شرط تشغيلي — بند 103)، فلا يدخله أثر حساب.
# دخل فعلاً بـPR#124 عبر `git add -A` وأُخرِج فوراً بعده.
.wrangler/
