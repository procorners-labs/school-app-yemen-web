# قياسُ أسباب الإخفاق بحقل `why` — وتسليمُ جلسة الوركر 2026-09-12

> **المُدخَل:** كونسولٌ حيٌّ لصقه المالكُ من منصّة المعلّم (مدرسة `abdaawatmuaz`) فيه ٥٠٢
> متكرّر على `POST /gas/teacher`، وفشلُ `addNewsMultiProtected` **بعد** اكتمال رفع مرفقاته،
> وسؤالٌ صريح: **«لماذا بعد كلّ هذه الإصلاحات ما زالت الأخطاء موجودة؟»**
>
> 🔴 **ولماذا هذا الملفُّ متتبَّعٌ لا في `local-reports`:** أرقامُه **لقطةُ نافذةٍ زمنيّةٍ
> زائلة** — سجلّاتُ Cloudflare لها مدّةُ احتفاظ، والنافذةُ نفسُها لا تُستعاد بعد انقضائها
> ⇒ **لا يُعاد إنتاجُها بتشغيل أمر.** (قاعدةُ موضع الكتابة في `~/CLAUDE.md`.)
>
> ⚠️ **وكلُّ رقمٍ هنا مقيسٌ في هذه الجلسة ومعه أداتُه — أعِد قياسَه ولا تُصدِّقه** (فئة بند 83-ب).

---

## ٠. الخلاصةُ في خمسة أسطر

1. **الإصلاحاتُ التي هبطت كانت صحيحةً كلَّها — ولم تكن إصلاحاتِ الجذر.** مطالبةُ الجذر
   حالتُها **`[❌ لم يُنفَّذ]`** منذ 09-03، و**١٠٨ دمجةً** جرت هنا بعدها بلا واحدةٍ تمسّ الإشباع.
2. 🎯 **و٩٥٪ من الإخفاق ليس عطبَ GAS بل إجهاضاً منّا** — مقيسٌ بحقل `why`، وصفرُ
   `upstream_status` في النافذة.
3. **والأداةُ التي تُثبت ذلك هبطت في `#241` (‏09-10) ولم يستعلمها أحدٌ حتى هذه الجلسة.**
4. 🎯 **والسببُ خلف السبب فئةٌ لا حالة:** «خانقٌ لا يُختَم عند الفشل يجعل الإشباعَ يُغذّي
   نفسَه» — رُصدت في **ثلاثة مستودعاتٍ لا تشترك في سطرِ كود**.
5. **وخطأُ `content.js: No Listener` ليس من كودنا إطلاقاً** — إضافةُ متصفّح، وسبق فرزُها 09-03.

---

## ١. الفئتان المخلوطتان في بلاغٍ واحد — وعلاجُهما متعاكس

| العرَض | الحكمُ المقيس | الأداة |
|---|---|---|
| `content.js: Uncaught Error: No Listener: tabs:outgoing.message.ready` | **إضافةُ متصفّح — خارج نطاقنا** | `grep` على `content.js` · `tabs:outgoing` · `No Listener` في `SchoolApp` ⇒ **صفرُ مطابقة**، **وضابطٌ معاكس** على سلسلةٍ موجودةٍ يقيناً أعطى مطابقاتٍ ⇒ المِجَسُّ يعمل. وفي `SchoolApp-gas` ورودُها **في وثائق فرزها وحدها** |
| ٥٠٢ على `POST /gas/*` | **عَرَضٌ حقيقيّ** | §٢ |

🔴 **وإخراجُ الأولى من البسط أوّلاً ليس تفصيلاً:** عدُّها ضمن الأعطال يضخّم البسطَ ويقود
إلى الرافعة الخطأ (نفسُ ما وقع في فرز 09-03). **والإثباتُ للمالك بخطوةٍ واحدة:** افتح
الصفحةَ في نافذةٍ خاصّةٍ أو عطّل الإضافات ⇒ **تختفي هي ويبقى ٥٠٢.**

---

## ٢. القياسُ الحاكم — نافذةٌ نظيفةٌ بضابطها

**النافذة:** `2026-09-12T12:05Z → 15:05Z` (٣ ساعات).
**نظافتُها مقيسة:** آخرُ بناءِ `main` عند **`11:59:13Z`** (‏`dde70e7a` · `b914550`) ⇒ **صفرُ
نشرٍ داخلها**، فلا تصفيرَ لحالة المنظّم وسطَها (‏النشرُ يُدخل بدءاً بارداً).
**الأداة:** `cloudflare-observability` · `ev='gas'` · تجميعٌ على `why`.

### ٢-أ · التوزيع

| `why` | العدد | النسبة | المعنى |
|---|---:|---:|---|
| `ok` | 1,022 | 67.0٪ | نجاح |
| 🔴 **`abort_budget`** | **479** | **31.4٪** | **أجهضناه نحن عند الميزانية** |
| `upstream_html` | 25 | 1.6٪ | صفحةُ اعتراضٍ من Google |
| `upstream_status` | **٠** | — | **صفرُ دالّةٍ أعادت حالةً خاطئة** |
| `transport` | **٠** | — | صفرُ فشلِ نقل |

**الإخفاق ٥٠٤ من 1,526 = ٣٣٪ · ومنه ٤٧٩ (٩٥٪) إجهاضٌ منّا.**
🎯 **أي أن الوركرَ لا يُخفق — بل يستسلم لخلفيّةٍ لا تردّ داخل ٢٤ ثانية.**
ومعدّلُ توليد التنفيذات المحتجَزة **≈١٦٠/ساعة**.

### ٢-ب · الضابطُ المعاكس — ولماذا لزم بديلاً عن الموصوف في المهارة

`uniq` **بلا `groupBys`** أسقط الاستعلامَ بخطإ تحقّقٍ في العميل (‏«expected number, received
string» على كلّ صفٍّ من السلسلة) ⇒ **الصيغةُ الموصوفةُ في `/measure-worker` غيرُ قابلةٍ
للتشغيل في هذا الشكل**. والبديلُ المطبَّق **أقوى دلالةً:** نفسُ الاستعلام مجمَّعاً على
**حقلٍ وهميّ** ⇒ **صفرُ دلو** (‏`Aggregation` فارغةٌ تماماً)، بينما `why` انقسم **ثلاثةَ
دِلاءٍ مسمّاةٍ بأعداد**. ⇒ **الحقلُ حقيقيٌّ والرقمُ صالحٌ للإبلاغ.**
⚠️ **وتجميعٌ على نفس الحقل يجعل `uniq(why)` = 1 في كلّ صفٍّ فيساوي الضابطَ** — فلو اعتُمد
لقُرئ «لا نتيجة» على قياسٍ سليم.

### ٢-ج · المستهلك

| `app` | `abort_budget` | النسبة |
|---|---:|---:|
| **`student`** | **318** | **66.4٪** |
| `teacher` | 134 | 28.0٪ |
| `home` | 27 | 5.6٪ |

🔴 **و`GAS.student = GAS.teacher` — نشرةٌ واحدةٌ تحمل المنصّتين** ⇒ **رشقاتُ إقلاع الطالب
تُجوّع المعلّم على نفس مشروع Apps Script.** وهو ما رآه المالكُ بعينه، ويكرّر حكمَ 09-03
بأرقامٍ جديدة.

### ٢-د · أعلى الدوالّ إجهاضاً

`getStudentBootBundle` **91** (١٩٪ من كلّ الإخفاق) · `studentHeartbeat` 37 · `getGrades` 35 ·
`getHomePageBundle` 26 · `getStudentNotificationBadge` 20 · `getHomeScheduleBundle` 20 ·
`checkAppVersion` 19 · **`checkPendingPlatformReviewRequest` 17** ·
`autoSaveGradesBatchProtected` 15 · `loginStudent` 14 · `registerDeviceToken` 14 ·
`getStudentReports` 14 · `getTeacherBootBundle` 13 · `handleTeacherLogin` 12.

---

## ٣. لماذا بقي العطلُ بعد كلّ الإصلاحات — ثلاثُ طبقات

| # | الطبقة | الشاهد |
|---|---|---|
| ① | **مطالبةُ الجذر لم تُنفَّذ قطّ** | `SchoolApp-gas/_planning/prompts/2026-09-03-EXEC-zombie-executions-and-review-throttle.md` ⇒ **`[❌ لم يُنفَّذ]`** في `origin/main` |
| ② | **وما هبط صحيحٌ ولا يمسّ الجذر** | **١٠٨ دمجةً** هنا منذ 09-03: حرّاسٌ · CSP · سجلّاتٌ · تقاعُدُ `/pricing` · وثائق |
| ③ | **والأداةُ بُنيت ولم تُقرأ** | حقلُ `why` في `#241` (‏`303b3f3` · **09-10**) — وهو الموجةُ ت-٤ من المطالبة نفسِها. **أوّلُ استعلامٍ له في هذه الجلسة** |

🎯 **والدرسُ التشغيليّ:** الحاجزُ كان **دليلاً بيد المالك** (لوحةُ `Executions`)،
**ومطالبةٌ معطَّلةٌ على دليلٍ لا يملكه منفّذُها تبقى معطَّلةً إلى الأبد بلا أن يحمرّ شيء.**

---

## ٤. 🎯 السببُ خلف السبب — فئةٌ في ثلاثة مستودعات

> **خانقٌ لا يُختَم عند الفشل يجعل الإشباعَ يُغذّي نفسَه.**

| الموضع | العلّة | الحال |
|---|---|---|
| `SchoolApp-gas/teacher/_js-platform-reviews.html:1652,1664` | الطابعُ **يُمحى عند الفشل** ⇒ يُعاد كلَّ إقلاعٍ بدل مرّةٍ كلَّ ٦ ساعات | **حيٌّ — ١٧ إجهاضاً/٣س مقيسة** |
| `SchoolAppyemen` · `UpdateChecker` | `putLong(KEY_LAST_CHECK)` **داخل `if(responseCode==200)` وحدَه** ⇒ من يفشل لا يُختَم له وقت | **حيٌّ ومنشورٌ على Play** |
| `YemenSchoolz/…/AppConfig.kt:228-269` · `syncIfNeeded()` | نسخةٌ طبق الأصل: `putLong` داخل `if(200)` و`if(parsed)` · نفسُ `6h` · نفسُ مهلة `8s` | 🔵 **خاملٌ** (‏معلَّقٌ في `init()` · التطبيقُ غيرُ منشور) ⇒ **صفرُ نداءٍ في النافذة** |
| الوركر | 🟢 **سليمٌ أصلاً** — لا إعادةَ على المهلة · و٥٠٢/٥٠٣/٥٠٤/٤٢٩ مستثناةٌ من الإعادة | — |

➕ **ومضاعِفٌ مستقلٌّ في الحالتين الأندرويديّتين:** مهلةُ العميل **٨ث** مقابل الميزانية
**٢٤ث** ⇒ العميلُ ينسحب **ولا يرى نتيجةً فلا يختم**، والوسيطُ يمضي فيُسجَّل `abort_budget`.
⇒ **حلقةٌ مغلقة: الفشلُ يمنع الختمَ، وغيابُ الختم يُنتج فشلاً.**

🎯 **وقيمةُ الموضع الثالث أنه خامل:** الفئةُ **لا تُنتج حالاتٍ جديدةً فقط، بل تنام في كودٍ
معطَّلٍ حتى يُفعَّل** ⇒ **قاعدةٌ واحدةٌ تُكتب الآن أرخصُ من ثلاث ترقيعاتٍ ومن رابعةٍ لم تُكتب.**

🔴 **وحدٌّ يمنع قلبَ القاعدة إلى ثقب:** الخَتمُ عند **الفشل العابر** صحيح، **والخَتمُ عند
الخطأ المنطقيّ يُخفي عطلاً دائماً ستّ ساعات.** ⇒ القسمةُ بـ`_csIsTransientErr` **هي القاعدةُ
نفسُها**، **وحارسُ الفئة يقيس الشرطَ لا وجودَ الختم** وإلّا خضّر على الصيغة الخاطئة.

---

## ٥. ما أُنجز في هذا المستودع — PR #272 (‏مفتوحةٌ لا مدموجة)

**التراجعُ إلى نسخةٍ بائتة عند `abort_budget` وحده** للقراءات العامّة في `API_CACHE_FNS`:
`API_STALE_MAX_S = 600` · ردٌّ 200 برأس `X-Api-Stale` · وسجلُّ `act:'stale'`.
**الأثرُ المتوقَّع ≈٤٦ من ٤٧٩ (١٠٪) بصفرِ حملٍ إضافيٍّ على GAS.**

🔴 **والفخُّ البنيويُّ الذي تحمله الدفعة — يُقرأ قبل أيّ «تضييقٍ آمن» لاحق:** الطزاجةُ كانت
مفروضةً بـ`caches.default.match` نفسِها. ومدُّ العمر إلى `ttl + 600` (كي يبقى البائتُ
**مطابَقاً**) **يُلغي ذلك الفرضَ الضمنيّ** ⇒ بلا بوّابةٍ صريحة يصير البائتُ **إصابةً طازجةً
تُخدَم بصمت**: الصفحةُ تعمل، والبياناتُ قديمة، ولا شيء يحمرّ. ⇒ `_apiCacheFreshness(fn, age)`
دالّةٌ نقيّةٌ تصنّف `fresh|stale|expired`، و`age < 0` ⇒ `expired` **fail-closed**.

⚠️ **وسجلُّ `ev:'gas'` يبقى `ok:false · why:abort_budget` عمداً** — لولاه لانخفض عدّادُ
الإشباع **بلا أن يتحسّن الإشباع**، وعليه يُبنى قرارُ نشرٍ حيّ.

**الحرّاس ١٣ فحصاً — ٦ سلوكيّةٌ بـ`vm` والباقي نصّيٌّ بحدِّه معلَناً — ومُثبَتةٌ بثلاث طفرات:**

| الطفرة | النتيجة |
|---|---|
| ردُّ `Cache-Control` إلى `ttl` وحده | ❌ **١** — «طزاجة+بيات» |
| تعميمُ الشرط إلى `!good && _acStale` | ❌ **٢** — الفحصُ **وضابطُه المعاكس** معاً |
| بوّابةٌ تُرجِع `fresh` للبائت | ❌ **٢** — ومنها «الطزاجةُ لكلّ دالّةٍ من جدولها» |
| **إعادةُ الصحيح** | 🟢 **`EXIT=0` · `RESULT: ✅ 40` · صفرُ ❌** |

### وتصحيحٌ توثيقيٌّ بلا سطرِ كود — ونقضُ فرضيّةٍ قبل البناء عليها

`CLAUDE.md` كان يقول «⇒ 503 نظيف لا 502 خام عند الاستنفاد» — **باطلٌ في الحالة المهيمنة**:
مسارُ الـ503 مشروطٌ بردٍّ **غيرِ JSON** ورسالةُ إجهاضنا **JSON صالحة** ⇒ يُخدَم **٥٠٢**.
🔴 **وظننتُ أوّلاً أن هذا يُفقد الجسرَ تراجعَه إلى IndexedDB — ونُقض بالقياس:**
`gas-bridge.js:109-110` يعامل **٥٠٢/٥٠٤ إشباعاً مثل ٥٠٣/٤٢٩ تماماً** منذ 2026-08-24 (`_sat`)
⇒ **صفرُ أثرٍ على المستخدم** ⇒ **صُحِّحت الوثيقةُ ولم يُغيَّر رمزُ الحالة** (تغييرُه تعديلٌ
بلا أثرٍ يبدو إصلاحاً).
➕ وصفّان في `.github/claude-context.md` (بند ⑧) — `claude[bot]` لا يرى `~/.claude/`.

---

## ٦. حدودُ هذه الجلسة — تُكتب مع أرقامها

- 🔴 **`abort_budget` يُثبت وجودَ مولِّد الاحتجاز لا حجمَ التضخيم.** إجهاضُ `fetch` لا يوقف
  Apps Script — هذا موثَّق. **وأن المُجهَضَ يجري إلى ٣٦٠ث غيرُ مقيس**، ودليلُه **لوحةُ
  `Executions`** وقد تكفّل المالكُ بفتحها. **`UNMEASURED` نتيجةٌ ثالثةٌ صالحة.**
- ⚠️ **نافذةُ ٣ ساعاتٍ ليست أساساً لمقارنةِ «قبل/بعد»** — قِيس في هذا المستودع **تقلّبٌ
  يوميٌّ ثمانيةُ أضعافٍ بلا أيّ تدخّل** ⇒ المعيارُ **نفسُ ساعات اليوم** لا نفسُ الطول.
- ⚠️ **ومتغيّرٌ جديدٌ في نفس اليوم يُعلَن:** **خمسُ نشراتِ GAS حيّة** (‏`teacher@920` ·
  `master-admin@170` · `home@133` · `cms@146` · `home-all-school@33`) جرت قُبيل النافذة بإذن
  مالكٍ مباشر ⇒ **أرقامُ النافذة بعدَ نشرٍ خلفيّ، لا على حالةٍ مستقرّةٍ منذ أيام.**
- 🔴 **وتدخّلان وقعا بعد النافذة — يُكتبان هنا لأن إغفالَهما يُنتج إسناداً كاذباً:**
  في **`2026-09-12T16:23:37Z`** نُشر `teacher@921` من جلسة `SchoolApp-gas` بإذن مالكٍ صريح
  في جلستها (‏`gas#1553` ⇒ `main@4ac0f00`)، **ودفعتُه الآليّةُ مسّت `frontend/teacher/index.html`
  (‏+٥٧/−١١) فأنتجت بناءَ وركرٍ حيّاً** (‏`30cead84` · `origin/main@0454619` · قِيس من
  `workers_builds_list_builds` لا من دعوى النظيرة).
  ⇒ **متغيّران جديدان بين نافذة القياس وأيّ نافذةٍ لاحقة:** منطقُ خلفيّةٍ تغيّر · **ونشرُ
  وركرٍ يُدخل بدءاً بارداً ويصفّر حالةَ المنظّم داخل الـisolate**.
  🔴 **فمن يقيس بعد هذا التاريخ ويقارن بأرقام §٢ لا يقيس أثرَ PR #272** — يقيس اتّحادَ
  ثلاثةِ تدخّلاتٍ ويُسنده إلى واحد. **والإسنادُ يلزمه نافذةٌ بينها تدخّلٌ واحدٌ فقط.**
- 🔴 **`npm run probe` لم يُشغَّل عمداً** — يطلق ٨٠ نداءً على مسارٍ مشبَعٍ فيقيس أثرَ نفسِه.
  **وقياسٌ يغيّر ما يقيسه ليس قياساً.** ولم يُضرَب `?action=health` إلّا **مرّةً واحدةً**
  للتحقّق من الحياة (‏200) — والمِجَسُّ جزءٌ من المقام.
- ⚠️ **وحصّةُ `student` مقيسةٌ من طرف الوسيط لا من العميل** — عميلُ الأندرويد ينسحب عند ٨ث،
  فعددُ محاولاته قد يكون **أكثرَ** لا أقلّ.

---

## ٧. ما ينتظر المالك — بندان

1. 🎯 **لوحةُ `Executions` لمشروع `teacher`:** كتلةٌ عند **٣٦٠ث** ⇒ الفرضيّةُ مؤكَّدةٌ
   وعلاجُ الجذر بلا نقاش. **وكلٌّ دون ٣٠ث ⇒ الفرضيّةُ تسقط ولا يُنفَّذ العلاج.**
2. **وقفةُ نشرٍ واحدة** ⇒ تُنفَّذ ت-٥ (المخرجُ المبكّر) وإكمالُ ت-١ على سطح `doGet` معاً.
3. **ودمجُ PR #272** — 🔴 **نشرٌ حيٌّ فوريّ، فلا يُدمَج بلا موافقةٍ لحظيّة.**

## ٨. ملكيّةُ ما بقي

| البند | المالك |
|---|---|
| ت-١ (خنقُ المراجعة) · ت-٥ (المخرجُ المبكّر) · أ-٣ (كاشُ `getStudentBootBundle`) | **`SchoolApp-gas`** — سُلّمت لجلسة `fix-class-section-filtering` برسالةٍ تحمل القياسَ كاملاً وتمييزَ السطحين |
| ختمُ `KEY_LAST_CHECK` + مواءمةُ المهلة | **`SchoolAppyemen`** — يلزمه **إصدارٌ على Play** ⇒ قرارُ مالك |
| قاعدةٌ وقائيّةٌ لـ`AppConfig.syncIfNeeded` | `YemenSchoolz` — بندٌ مسجَّلٌ، **ولم يُصلَح عمداً**: مسارٌ ميّتٌ لا يُثبِته ضابطٌ يجري |
| PR #272 | **هذا المستودع** — منتهيةٌ وتنتظر الدمج |

📖 الخطّةُ الكاملة: `~/.claude/plans/steady-wobbling-toucan.md` ·
والبندُ المسجَّل: `abort-budget-479-confirms-zombie-generator` في `~/.claude/health/pending.json`.

---

## ٩. التسليمُ المشترك — جلستا `SchoolApp` و`SchoolApp-gas`

> بطلبِ المالك أن تُسلّم الجلستان معاً. **وكلُّ دعوى الجلسةِ الأخرى أُثبتت من هذا المستودع
> بالأداة لا بالنقل** — حيث أمكن ذلك، وما لم يمكن سُمّي.

### ٩-أ · ما أُنجز

| الجلسة | ما دُمج | نشرٌ حيّ؟ |
|---|---|---|
| `SchoolApp-gas` | `#1553` ⇒ `main@4ac0f00` — الشعبُ تتبع الفصل (‏`getMySectionsForGradeSubject` كانت تتجاهل معاملَ الفصل لمسارَي `isAdmin`/`supervisor` فتعرض اتحادَ شعب المدرسة · ١٢ موضعَ استدعاءٍ في ٥ ملفّات) | **نعم** — `teacher@921` بإذن مالكٍ **في جلستها** |
| `SchoolApp-gas` | `#1554` ⇒ `main@d27dbe8` — وثيقةُ الأدوار (تصحيحُ تسع نقاطٍ مقيسة) | **لا** |
| `SchoolApp-gas` | `#1555` + `#1556` ⇒ `main@9134018` — مصفوفةُ القدرات والدورُ المركَّب | **نعم** — `teacher@922` |
| `SchoolApp` (هذه) | **PR #272 مفتوحةٌ لم تُدمَج** — `HEAD = 3975ae0` · `MERGEABLE` · أربعةُ فحوصٍ `SUCCESS` | **لا — بقرار المالك** |

🔴 **تصحيحٌ بعد أوّل كتابةِ هذا القسم: تدخّلٌ ثالثٌ وقع فعلاً — `teacher@922`.** أبلغتني
النظيرةُ أن المالكَ أذِن لها بنشرةٍ ثانيةٍ بعد أن كانت أجابتني «صفرُ نشرةٍ بعد @921»
(‏وكان صادقاً لحظتَه). **ودُمجت `#1555` و`#1556` ⇒ `main@9134018` ⇒ `teacher@922`.**

| # | ما فيه | النشر |
|---|---|---|
| `#1555` | **مصفوفةُ قدراتٍ ومُحكِّمٌ واحد** — ٢٤ مُسنِداً متفرّقاً صارت مصدراً واحداً · **صفرُ تغييرٍ سلوكيّ** (‏١٤٤ تركيبةَ «قدرة × دور» متطابقة) | **`teacher@922`** |
| `#1556` | **الدورُ المركَّب** — القدراتُ **فوق** التكليف لا بدلاً منه · وإصلاحُ معجم الأدوار | **`teacher@922`** |

🎯 **والفرقُ الحاكمُ للقياس — ولذلك يُكتب بدل أن يُطوى:** الدفعتان **خلفيّةُ GAS محضة**
(‏`.js` فقط) ⇒ صفرُ فرقٍ في `frontend/` ⇒ **صفرُ دفعٍ وصفرُ بناءِ وركر**.

**وأُثبت هذا من هذا المستودع لا نقلاً:** `origin/main` **ثابتٌ عند `0454619`** بعد `#1553`
ولم يتحرّك بعد `#1554` ولا بعد `#1555`/`#1556` (‏وتشغيلةُ `34705992118` عندها تنتهي بـ
`No frontend changes — skipping push`). ⚠️ **والبناءان الوحيدان بعده على فرع
`fix/stale-if-error-on-abort-budget` — أي كوميتا وثائقي نفسِها**، لا على `main`.
🔴 **وهذا فخُّ قياسٍ يُقال لمن يقرأ لاحقاً: «بناءٌ بعد التاريخ س» ليس «نشراً» — `branch: main`
وحدَها إنتاج، وبناءاتُ الفروع رفعُ نسخةٍ لا طرح.** ومن عدّها نشراً نسب إلى نفسِه تدخّلاً.

⇒ **الحصيلةُ الدقيقة، ولا تُختصَر إلى رقمٍ واحد:**

| الطبقة | عددُ التدخّلات بعد نافذة §٢ |
|---|---|
| **الوركر** (‏موضوعُ قياس §٢ و PR #272) | 🔴 **ثلاثةُ نشراتٍ حيّة** — انظر التصحيحَ أدناه |
| **نشرةُ Apps Script** (‏موضوعُ أ-٣ وت-٥) | 🔴 **أربعة** — `@921` · `@922` · `@923` · `@924` (‏~23:52Z · `main@6624f51`) |

⚠️ **والأخيرةُ تمسّ مسارَين لا مسارَاً:** `GAS.student = GAS.teacher` ⇒ **`/gas/student`
يُخدَم من `@924` نفسِها** ⇒ **أساسُ `getStudentBootBundle` (٩١ إجهاضاً) يسبق الآن أربعَ
نشراتٍ خلفيّة.** و`origin/main` هنا **ثابتٌ عند `9ab017f`** ⇒ **صفرُ نشرِ وركرٍ خامس**
(البناءُ الوحيدُ بعده على فرع هذه الدفعة لا على `main`).

🔴 **تصحيحٌ ثانٍ (‏19:40Z) — ورقمُ الوركر الذي كتبتُه مرّتين كان خطأً، والخطأُ منّي:**
كتبتُ «الوركر: اثنان» وهو **يخلط نشرةَ GAS ببناءِ وركر**. والمقيسُ من
`workers_builds_list_builds` — **ثلاثةُ بناءاتٍ على `branch: main` كلُّها `success`:**

| البناء | الوقت | الكوميت |
|---|---|---|
| `30cead84` | `16:23:37Z` | `0454619` ← `[4ac0f00]` |
| `951d587e` | **`18:41:05Z`** | `fed4dcd` ← `[17c2040]` |
| `400417ee` | **`19:31:30Z`** | `34a1c91` ← `[b3a7a86]` |
| `d8341236` | **`20:31:32Z`** | `9ab017f` ← `[116c4d0]` — 🟢 **وهذا أوّلُ ما أُبلغتُ به بمخرَج الدفع لا بنوع الملفّ**، وطابق القياسُ الإبلاغَ حرفياً (`1 file changed, 32 insertions(+)`) |

**والفرقُ المدفوع في الأخيرتين:** `frontend/teacher/index.html` **+١٢٨** ·
`frontend/master-admin/index.html` **+١٠** · وصفرُ ملفٍّ خارج `frontend/`.

🎯 **والدرسُ أن هذا وقع بعد أن كتبتُ قاعدتَه بيدي:** جلسةٌ نظيرةٌ أبلغتني «تمسّ
`master-admin/_MasterScope.js` وحدَه ⇒ **لا أثرَ على الوركر**» — **واستدلالُها بنيّة الدمج
لا بمخرَج الدفع**، وهو بعينه ما ينهى عنه `CLAUDE.md` §صمتُ الدمج: **«اسأل النظيرةَ عن
مخرَج الدفع لا عن نيّة الدمج»**. ⇒ **دفعةٌ خادميّةٌ في مستودعٍ آخر أنتجت هنا نشرَ إنتاجٍ
حيّاً مرّتين** — لأن البناءَ يُعيد توليدَ `frontend/` كلِّها، فأيُّ تغيّرٍ في مخرَجها يُدفَع.
🔴 **والقاعدةُ المستخلَصة: «تغييرٌ خادميٌّ محض» في `SchoolApp-gas` لا يعني «صفرَ نشرِ وركر»
— الحكمُ مخرَجُ `rsync` وحدَه، ويُقاس من `branch: main` في قائمة البناءات لا يُستنتَج.**

🔴 **وأثرُه العمليّ على أ-٣ تحديداً:** `getStudentBootBundle` **يُخدَم من نشرةِ `@922`**
⇒ **أساسُها المقيس (٩١ إجهاضاً) يسبق نشرتين خلفيّتين** ⇒ **يُعاد قياسُ الأساس قبل أيّ
حكمٍ على كاشٍ خادميٍّ يُضاف لها**، ولا يُقارَن بأرقام §٢-د مباشرةً.

### ٩-ب · ما بقي — ولا تُقرأ أيُّ خانةٍ منه «قيد التنفيذ»

| البند | الحال المقيس | المانع |
|---|---|---|
| 🎯 **ت-٥ · المخرجُ المبكّر في GAS (الجذر)** | **لم يبدأ** | **وقفةُ مالكٍ لحظيّة** + دليلُ لوحة `Executions` |
| **أ-١ · خنقُ المراجعة** | **لم يبدأ** — ولا مانعَ تقنيّاً | الأولويّةُ وحدَها |
| **أ-٣ · كاشُ `getStudentBootBundle`** | **لم يبدأ** | **قرارُ أولويّةٍ من المالك** (نظامُ الأدوار أوّلاً) — لا رفض |
| **نظامُ الأدوار الموحَّد** | 🟢 **أُنجز ونُشر بعد كتابة هذا الجدول أوّلاً** — `#1555` (مصفوفةُ قدراتٍ ومُحكِّمٌ واحد · صفرُ تغييرٍ سلوكيّ) و`#1556` (الدورُ المركَّب: القدراتُ **فوق** التكليف لا بدلاً منه) ⇒ `teacher@922`. **وكان مكتوباً هنا «لم يبدأ كوداً» وبطَل** | — |
| **PR #272** | **منتهيةٌ تقنياً** | **الدمجُ نشرٌ حيٌّ فوريّ** ⇒ موافقةٌ لحظيّة |
| **ختمُ `KEY_LAST_CHECK`** (‏`SchoolAppyemen`) | **لم يبدأ** | **إصدارٌ على Play** ⇒ قرارُ مالك |

🔴 **وصياغةُ الحال بلا تخفيف: الإشباعُ لم يُعالَج.** ما نُشر اليومَ **لا يخصّ الإشباعَ
بشيء** — إصلاحُ عرضٍ وبيانات. و**الجذرُ قائم**، ودليلُه الحاسمُ **`UNMEASURED`**.

### ٩-ج · 🎯 الفئةُ اتّسعت بقياس النظيرة — من «الخنق» إلى «رمزٌ سليمٌ بلا مستهلك»

الفئةُ التي سُمّيت في §٤ («خانقٌ لا يُختَم عند الفشل») **حالةٌ خاصّةٌ من أعمَّ منها**،
وقياسُ النظيرة أضاف مثالين مستقلَّين تماماً:

| المثال | الوصف |
|---|---|
| `_getRealSectionsMap()` | تُنتج الخريطةَ **الصحيحة** — و**تُحجَب عن العميل** فيُخدَم الاتحادُ الخطأ |
| `_tcIsMedia` · `_tcIsActivitiesRole` | **مُسنِدان بصفرِ مستدعٍ** ⇒ دورُ الوسائط **بلا حارسٍ موجبٍ واحد** |
| `_csIsTransientErr` | قائمةٌ جاهزةٌ **لا يستعملها** خانقُ المراجعة الذي يحتاجها |

🎯 **والجامع: «الأداةُ قائمةٌ ولا تُعطى لمن يحتاجها»** — وهو توصيفُ النظيرة، وأدقُّ من توصيفي.

### ٩-و · 🔴 وجهٌ ثالثٌ للفئة يغيّر **تفسيرَ** رقم §٢ لا الرقمَ نفسَه

قِيس في جلسة `arabic-merge-status-update` (‏2026-09-12 · بلاغُ المالك «المديرُ لا يرى خبرَه»)
أن **٧٤ نداءَ `callServer` من ٢٣٩** في `teacher/*.html` تُمرَّر **بمعالجِ نجاحٍ وحدَه**،
و`callServer` يوصل فشلَ النقل إلى **المعالج السادس** ⇒ **عند ٥٠٢ لا يقع شيءٌ إطلاقاً**.
(‏**حدٌّ أدنى** — المِجَسُّ تقريبيّ. والبند: `callserver-calls-without-error-handler`.)

🎯 **وأثرُ ذلك على قراءة هذه الوثيقة مباشر:** الـ**٤٧٩ إجهاضاً/٣ ساعات** في §٢ **ليست
«بطئاً» ولا «رسالةَ خطأ»** — تُترجَم عند المستخدم إلى **«لا بيانات»** في **٧٤ موضعاً على
الأقلّ**. ⇒ **العَرَضُ المُبلَّغُ يصير «الصلاحياتُ خاطئة» أو «الخبرُ لم يُحفظ»**، فيُوجَّه
التشخيصُ إلى الصلاحيات بدل الحمل. **وهذا يفسّر لماذا بقي الإشباعُ تسعةَ أيامٍ بلا علاج:
بلاغاتُه لا تصل بوصفها إشباعاً.**

🔴 **والوجهُ الثالثُ للفئة يُصاغ صراحةً:** ①  خانقٌ **يُمحى** عند الفشل · ②  رمزٌ سليمٌ
**بلا مستهلك** · ③ **ومسارُ فشلٍ غيرُ مكتوبٍ أصلاً**. **والجامعُ الأعمق: المسارُ الذي
لا يُكتَب هو مسارُ الفشل** — النجاحُ يُعالَج دائماً، والفشلُ يُهمَل أو يُساء استعمالُه.

🟢 **وعلاجُ الحالة المبلَّغة جاء صحيحاً في اتجاهه:** معالجُ خطأٍ برسالةٍ صريحةٍ **وزرُّ
إعادةٍ بإرادة المستخدم**، **وبلا مؤقّتٍ تلقائيّ عمداً** — لأن ٥٠٢ هنا **إجهاضٌ عند سقف
ميزانية الوركر بينما GAS يواصل التنفيذ**، فالإعادةُ التلقائيّة **تُضاعف الحملَ المسبِّب**.
⇒ **وهذا يطابق عقدَ الوركر:** الإشباعُ لا يُعاد عليه، و`_gasShouldRetry` لا يعيد على المهلة.

🟢 **ونُشر حيّاً في `teacher@924` (‏~23:52Z) — وأثرُه على قراءة بلاغاتٍ قادمة:** كلُّ ٥٠٢
على **مسار قائمة الأخبار وحدَه** صار يُعرَض رسالةً صريحة. ⇒ **بلاغاتُ الإشباع من ذلك
المسار ستصل بوصفها ازدحاماً لا «صلاحياتٍ خاطئة».**
🔴 **وموضعٌ واحدٌ من ٧٤ (حدٌّ أدنى)** — **فالبقيّةُ ما زالت تترجم كلَّ إجهاضٍ إلى «لا
بيانات»**، والانحيازُ في البلاغات **قائمٌ ولم يزُل**. ⇒ **لا يُقرأ هذا إغلاقاً للفئة.**

### ٩-د · وسؤالُ النظيرة عن نظيرٍ في الوركر — الجوابُ مقيسٌ، والفجوةُ أضيقُ ممّا خُشي

**سُئلتُ:** هل في الوركر قرارٌ يملكه GAS ومكرَّرٌ بلا مصدرٍ مشترك؟ **نعم — واحدٌ:**
`BH_LOGIN_FNS` (`worker/school-app-proxy.js:246`) أربعةُ أسماءِ دوالِّ دخولٍ مكتوبةٌ هنا،
وتُمنح نافذةَ قبولٍ أوسع (`BH_LOGIN_WAIT_MS`) ⇒ **إعادةُ تسميةِ أيٍّ منها في GAS تُسقط
الامتيازَ صامتاً**: الدخولُ يأخذ الانتظارَ العاديّ فيُرفَض أكثرَ في أسوأ لحظة.

🟢 **وخلافاً لما افترضتُه أوّلاً: الحارسُ قائمٌ ويفعل ما يدّعيه.** `worker/test-routes.js:1642-1676`
يقرأ **`SchoolApp-gas/teacher/*.js` فعلاً** ويطابق كلَّ اسمٍ بـ`function <name>(`، وله
`SKIPPED` **صريحة** عند غياب المستودع لا نجاحٌ صامت. **وجرى عندي وخضرّ:**
«قُرئ مصدر GAS فعلاً» ✅ · «كلّ اسمٍ له تعريفٌ حيّ في GAS» ✅.

🔴 **والفجوةُ الباقيةُ واحدةٌ ومحدَّدة: الفحصُ يعتمد وجودَ المستودع الشقيق على القرص.**
وفي CI هذا المستودعِ **العامِّ** يكون `SchoolApp-gas` **خاصّاً وغيرَ محضَر** ⇒ **`SKIPPED`**
⇒ **إعادةُ التسمية لا يراها CI إطلاقاً**، ولا تُكتشف إلّا حين يشغّل إنسانٌ أو جلسةٌ البوّابةَ
على جهازٍ يحمل المستودعين. ⇒ **حارسٌ حقيقيٌّ بتغطيةٍ ظرفيّة، لا حارسٌ أجوف** — والفرقُ يُقال.
🟢 **والحلُّ نمطٌ مُثبَتٌ في هذا المستودع نفسِه:** `worker/edge-cache-contract.json` وُجد
لهذه العلّة حرفياً (‏«إعادةُ تسميةٍ هناك **تُبرّد الكاشَ صامتاً**… والاتّجاهُ الوحيدُ الممكن:
هذا المستودعُ عامّ فيَجلب مستهلكُه الملفَّ بلا توكن»). ⇒ **نشرُ `BH_LOGIN_FNS` في عقدٍ
مقروءٍ آلياً يجعل CI الـGAS — وهو الذي يملك المصدر — قادراً على الإحمرار.**
📌 **وهذا خارج نطاق الخطّة المعتمَدة، فسُجّل بنداً ولم يُنفَّذ.**

### ٩-هـ · درسُ حرّاسٍ جاء من عطلٍ وقع بالنظيرة — يُقرأ قبل كتابة أيّ حارسٍ هنا

رصدت النظيرةُ أن `clasp-deploy-guard` عندها **يطابق مسارَ `_build/deploy*` في نصّ الأمر
بصرف النظر عن الفعل** ⇒ يحجب `sed`/`grep` — أي **قراءةً محضة**. 🔴 **وضررُه أنه يُعلّم
الالتفاف، ووقع بها فعلاً:** تراجعت عن طفرةٍ عبر `cmd.exe` فمُحيت تعديلاتُها غيرُ الملتزَمة.
⇒ 🎯 **التضييقُ بالفعل لا بالمسار.**

**وحرّاسُ هذا المستودع مقيسةٌ على هذا المعيار، والحكمُ ليس واحداً فيها:**

| الحارس | نطاقُه | الحكم |
|---|---|---|
| `measure-guard.js` | **الفعل** (أنبوبٌ يبتلع رمزَ الخروج · راية `-w`) | 🟢 صحيح — **وحجبني مرّتين اليومَ بحقّ** |
| `gh-repo-guard.js` | **ثلاثيّةٌ ضيّقة**: فعلٌ مُغيِّرٌ + رقمٌ صريح + غيابُ `--repo` | 🟢 صحيح — والقراءةُ لا تُحجَب أبداً |
| `protect-deploy-ids.js` | مساره `worker/*.js` **ثمّ يقارن مجموعةَ رموز `AKfycb…` سلوكياً** | 🟢 صحيح — المسارُ نطاقٌ والقرارُ على الأثر |
| `frontend-guard.js` | **المسارُ وحدَه** (`frontend/`) | 🟢 **صحيحٌ استثناءً — والمسارُ نفسُه هو الثابت**: الشجرةُ كلُّها مخرَجٌ مولَّدٌ يُدهَس، فلا فعلَ مشروعاً فيها أصلاً |

⇒ **القاعدةُ ليست «لا تحرس بالمسار» بل «لا تحرس بالمسار إلّا حين يكون المسارُ نفسُه هو
الثابتَ المحروس».** وما عداه يحجب قراءةً بريئةً فيُعلّم الالتفاف.
