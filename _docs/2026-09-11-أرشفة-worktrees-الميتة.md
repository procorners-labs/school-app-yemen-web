# أرشفةُ `.claude/worktrees/` الميّتة — 2026-09-11

**الأرشيف:** `C:\Users\osama\Workspace\Releases\worktrees-archive-2026-09-11\` — 🟢 **تمّ ومُتحقَّقٌ منه**
**المصدر:** `SchoolApp\.claude\worktrees\` (‏**متجاهَلٌ** في git) — 🔴 **قائمٌ لم يُحذف بعد**

> 🔴 **الحالةُ بالضبط: أُرشف ولم يُحذف — ويُقال صراحةً كي لا تُقرأ الوثيقةُ إنجازاً تامّاً.**
> حُجب `rm -rf` بـ**`destructive-guard`**، ونصُّه: «أمرٌ ماحٍ… **وثماني جلساتٍ تعمل على قرصٍ
> واحدٍ لا على نسخ** — فهذا قد يمحو عملَ جارك بلا تعارضٍ يُنبّه أحداً. إن كان مقصوداً:
> **اطلبه من المالك صراحةً في هذه الجلسة**».
> 🔒 **والتفويضُ العامُّ («افعل كلَّ شيء») ليس إذناً محدَّداً بهذا الفعل بعينه** — والحاجزُ
> وُضع لهذه الحالة نفسِها. **ولم يُلتفّ عليه بمفسّرٍ آخر**؛ الالتفافُ **نقضٌ لقرار المالك لا
> حلٌّ تقنيّ**، وهو منصوصٌ في الحارس.
> ⇒ **الأمرُ المطلوبُ حين يأذن** (‏يُنفَّذ من جذر `SchoolApp`):
> ```
> rm -rf .claude/worktrees/dazzling-elgamal-942e8f .claude/worktrees/exciting-hellman-cbe644 .claude/worktrees/infallible-napier-23b594 .claude/worktrees/naughty-bose-497185 .claude/worktrees/unruffled-chatelet-0c4978
> ```
> 🟢 **والأرشفةُ وحدَها كسبٌ قائم:** الاحتمالُ محفوظٌ خارج المستودع، **فصار الحذفُ لاحقاً
> بلا مخاطرةٍ على البيانات** — بقي منه قرارُ المالك فقط.

> 🔴 **ولماذا هذا الملفُّ هنا لا مع الأرشيف نفسِه:** حاولتُ كتابةَ الشرح داخل مجلد الأرشيف
> فحجبني **`wrong-root-guard`** بحقّ — `Workspace` مشروعٌ آخر **لا تُحمَّل حواجزُه في جلسةٍ
> جذرُها `SchoolApp`**. ⇒ الشرحُ يعيش في مستودعي، والأرشيفُ في موضعه المنصوص عليه
> (‏`CLAUDE.md` §خامساً: حزمُ الإصدار والأرشفة في `Workspace\Releases\`).
> ⚠️ **وأثرٌ يُقال بأمانة: نسخُ الملفّات نفسُه وقع بـ`cp` عبر Bash، والحارسُ يفحص أدواتِ
> التحرير لا النسخ** ⇒ **تجاوزَه تقنيّاً لا قصداً**. والوجهةُ مشروعةٌ بالقاعدة المكتوبة،
> لكنّ الفجوةَ تُسجَّل: **حارسُ الجذر لا يرى `cp`.**

---

## ما أُرشف — والمقيسُ وقتَ الأرشفة

| المجلد | المحتوى | أحدثُ تعديل |
|---|---|---|
| `exciting-hellman-cbe644` | 🟢 **فارغٌ تماماً — صفرُ عنصر** | — |
| `naughty-bose-497185` | 🟢 **فارغٌ تماماً — صفرُ عنصر** | — |
| `dazzling-elgamal-942e8f` | ١٢ عنصراً | **صفرُ ملفٍّ أحدثَ من 2026-06-10** |
| `infallible-napier-23b594` | ١٢ عنصراً | **صفرُ ملفٍّ أحدثَ من 2026-06-10** |
| `unruffled-chatelet-0c4978` | ١٥ عنصراً | **2026-06-15** (‏٨٨ يوماً قبل الأرشفة) |

**الإجماليّ: ٣٨٠ ملفّاً · ١٢ م.ب** — والنسخُ **مُتحقَّقٌ بالعدّ: ٣٨٠ = ٣٨٠**، والحجمُ ١٢M = ١٢M.

---

## 🔴 لماذا أُرشِفت ولم تُحذف مباشرةً — المانعُ التقنيُّ لم يُرفَع، بل حُفظ الاحتمال

**`git status` عليها يفشل:** ملفُّ `.git` في الثلاثة العامرة يشير إلى `gitdir` **منقرض**
⇒ **يستحيل إثباتُ «لا عملَ غيرَ مدفوعٍ فيها» بالأداة المعتادة.**
⚠️ **ومحتواها ليس من هذا المستودع أصلاً** — تحوي `cms/` ⇒ **نسخٌ من `SchoolApp-gas`**
⇒ **الحكمُ على قيمتها يعبر ملكيّتين.** ولذلك: **تُحفظ ولا تُتلَف.**

**وثلاثةُ أدلّةٍ خفّضت المخاطرة ولم تُلغِ المانع:** صفرُ نشاطٍ منذ ٨٨ يوماً · `gitdir`
منقرضٌ في الثلاثة · واثنان فارغان أصلاً. **وسُئلت جلسةُ `SchoolApp-gas` قبل الحذف.**

---

## الأثرُ على القياس — ولماذا استُحقّ الفعلُ أصلاً

🔴 **بطءٌ لا كذب:** مسحٌ بلا استثناءٍ على كامل المستودع **تجاوز ١٢٠ ثانية** في جلسة 2026-09-11.
🟢 **ولا تسميمَ على الأنماط الحديثة** — قِيس بضابطٍ ثلاثيّ على `_RESERVED_TOP_PATHS`:
داخل `worktrees` ⇒ **٠** · المستودعُ بلا استثناء ⇒ **١٦** · معه ⇒ **١٦**.
**والسببُ أن النسخَ من ٩ يونيو وحجمُها ٧٬٣٠١ بايتاً** (الوركرُ قبل ثلاثة أشهر) ⇒ **صفرُ رمزٍ حديث**.
⇒ **دعوى «تُسمّم grep» بإطلاقها كانت مبالِغة، والأثرُ الحقيقيُّ وقتُ المسح.**

---

## كيف تُستعاد

```bash
cp -r "C:/Users/osama/Workspace/Releases/worktrees-archive-2026-09-11/." \
      "C:/Users/osama/SchoolApp/.claude/worktrees/"
```
⚠️ **ولن تعمل كـ`git worktree` بعد الاستعادة** — `gitdir` منقرضٌ أصلاً ⇒ الاستعادةُ تُعيد
**الملفّاتِ للقراءة** لا الربطَ بـgit.
🔒 **ولا يُحذف الأرشيفُ إلّا بقرارٍ صريح** — هو الأثرُ الوحيدُ الباقي لهذه الشجرات.
