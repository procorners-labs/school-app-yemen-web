# تسليمُ جلسة `fix-worker-repo-coordination` — 2026-09-06

> جلسةُ `SchoolApp` (الوركر). كلُّ رقمٍ هنا مقيسٌ في هذه الجلسة بمصدره.
>
> 🟢 **دُمجت `2026-09-06T16:47Z` (‏`#211`) — واللافتةُ أدناه تاريخٌ لا أمر.**
> ~~🔴 وهذه الوثيقةُ على فرعٍ بـPR مفتوحٍ لا مدموج — الدمجُ هنا يَنشُر الوركر حيّاً
> ويُفسد نافذةَ قياسٍ مفتوحة. تُدمَج **بعد `2026-09-07T14:00Z`**.~~
>
> 🔴 **ولماذا تُشطَب ولا تُحذَف:** حذفُها يُخفي أن قيداً كان قائماً ولماذا سقط؛ وبقاؤها
> بلا شطبٍ **أخطر** — لافتةٌ في رأس وثيقةٍ **مدموجةٍ على `main`** تُقرأ أمراً قائماً لا
> مسوّدة. **والبائتُ المدموجُ أخطرُ من البائت المفتوح**، لأنه يُقرأ مرجعاً.
> سقط القيدُ لأن نافذةَ القياس **استُخرجت قيمتُها** (§٢) وأذن المالكُ بالدمج
> `2026-09-06`؛ و`#212` نُشرت وتُحقّق منها حيّاً في اليوم نفسِه.

---

## ١) ما شُحن — `SchoolApp#210` · `main@7832126` · نُشر `2026-09-06T13:16:01Z`

بصمةُ مفتاحٍ (`_apiKeyFp` · FNV‑1a ⇒ حقل `k`) في سطرَي `hit` و`store/skip` من `ev:'apicache'`.
**صفرُ تغييرٍ في سلوك الكاش** — لا مفتاح، ولا `ttl`، ولا قائمةٌ بيضاء.

**لماذا:** عائقُ بندَي كاش الحافّة كان «توزيعُ `argsKey` المميّزة»، و**قِيس أنه مستحيلٌ
بالأدوات القائمة** — الأسطرُ تحمل `app`+`fn` بلا هويّةِ مفتاح ⇒ البندان إمّا مفتوحان أبداً
أو يُغلقان بلا دليل. **بصمةٌ لا قيمة**: `argsKey` يحمل `schoolId` واسمَ الصفّ والشعبة.

**الحارس** (`worker/test-routes.js`) يستخرج الدالّة من مصدر الوركر ويشغّلها، ويفرض ألّا يظهر
`argsKey` **عارياً** في أيّ نداءِ سجلّ. **وطفرتان أُدخلتا فعلاً فحمّرتا ثمّ رُوجعتا.**
🔴 **وخطأٌ في الحارس نفسِه يُسجَّل:** صياغتُه الأولى «صفرُ نداءٍ يحمل `argsKey`» حمّرت على
الاستعمال **المشروع** `_apiKeyFp(probe.argsKey)` — **حارسٌ يمنع الهدفَ الذي كُتب لحمايته**.
صُحّح إلى «لا يظهر إلّا ملفوفاً بالبصمة».

---

## ٢) القياسُ المتبقّي — 🟢 **قِيس 2026-09-06T16:4xZ · والنافذةُ استُخرجت قيمتُها**

> 🔴 **هذا القسمُ كُتب قبل القياس، وصار عنوانُه «لم ينضج» بائتاً في اليوم نفسِه.**
> يُصحَّح ولا يُحذَف: بقاءُ الشرط والنافذة مكتوبَين هو ما يجعل الرقمَ أدناه مقروءاً.

**الاستعلام:** `uniq(k)` مقابل `count()` مجمَّعاً بـ`fn` على `ev = apicache`، نافذةُ ٢٤س.
**النافذة المعلَنة:** `2026-09-06T14:00Z → 09-07T14:00Z` — أُعلنت على الجلسات الستّ وأكّدت
كلُّها صفرَ دمج.

### 🟢 النتيجة — أُخذت مستقلّاً على `14:00Z → 09-07T00:00Z`

| `fn` | `uniq(k)` | `uniq(zzNoSuchField)` — الضابط | `count()` |
|---|---|---|---|
| `getHomeScheduleBundle` | **33** | **1** | 167 |
| `getHomePageBundle` | **2** | **1** | 167 |
| `getStudentSchoolBrand` | 1 | 1 | 88 |
| `getTeacherSchoolBrand` | 1 | 1 | 8 |

⇒ **شرطُ القبول مستوفىً بطرفيه:** `uniq ≥ 2` متحقّق **والضابطُ الوهميّ ثابتٌ على 1**
⇒ الحقلُ موجودٌ ويحمل قيماً متمايزةً فعلاً، لا دلوَ null. **والأداةُ حيّةٌ لا خامدة.**

**وما يقوله الرقمُ عن التجزئة:** ‏٣٣ مفتاحاً لـ١٦٧ نداءً على `getHomeScheduleBundle`
⇒ التجزئةُ حقيقية، والرافعةُ **`ttl` أطول لا مفتاحٌ أوسع** (إسقاطُ `class`/`section`
يخلط جداولَ فصولٍ مختلفة). و`getHomePageBundle` عند مفتاحين لـ١٦٧ ⇒ التجزئةُ ليست علّتَه،
فيُبحَث في `ttl` مقابل تباعدِ الزيارات وفي إخلاء الحافّة.

**⚠️ وثلاثةُ حدودٍ تُقال ولا تُطوى:**
1. 🔴 **البراندان عند `uniq = 1` والضابطُ عندهما `1` أيضاً** ⇒ **القياسُ عاجزٌ عن التمييز
   هناك بنيويّاً**: «مستأجرٌ واحدٌ مشروع» و«حقلٌ لا يصلهما» يعطيان الرقمَ نفسَه.
   ⇒ **لا يُقرآن نجاحاً ولا فشلاً**؛ يلزمهما حركةُ مستأجرٍ ثانٍ لا استنتاجٌ أذكى.
2. **فرقُ عدٍّ غيرُ محسوم:** جلسةٌ نظيرة قاست `getHomePageBundle = 164` مقابل `167` هنا
   (نافذتُها انتهت `20:00Z`). تفسيرُ «ثلاثةُ أحداثٍ بعد `20:00Z`» **متّسقٌ ولم يُعزَل**
   ⇒ يبقى مرصوداً لا مُفسَّراً. **وطيُّ فرقٍ صغيرٍ بلا تفسير هو ما يُنتج ثقةً في غير محلّها.**
3. **الحركةُ صمتت بعد `16:24Z`** ⇒ بقيّةُ النافذة لا تضيف على هذا المنوال.

🔴 **ومقتضى التجميد سقط بهذا** — النافذةُ لم يعد لها ما تحميه، وأذن المالكُ بالدمج
2026-09-06. **فلا يُقرأ عنوانُ هذه الـPR قيداً قائماً؛ هو سجلٌّ لا حالة.**
📖 والرقمُ محفوظٌ أيضاً في تعليقٍ على الـPR (‏`issuecomment-5560616270`) — كُتب هناك
أوّلاً لأن احتفاظَ سجلّات Cloudflare **٧ أيامٍ سقفاً**: النافذةُ تفنى والرقمُ يُفقَد.

🔴 **شرطُ القبول — ولا يُتجاوَز:** `uniq(k) = 1` **ليست دليلاً على وجود الحقل**؛ حقلٌ لا وجودَ
له يعيد `1` أيضاً (دلوُ الـnull). المقبولُ `uniq ≥ 2`، **ومع ضابطٍ وهميٍّ في نفس الاستعلام**.
🟢 وأُثبت حيّاً أن الميزةَ ليست خامدة: `act:store` ⇒ `n=3` و`uniq(k)=3` والضابطُ ثابتٌ على 1.

🔴 **ونافذتا حركةٍ صناعيّةٍ تُطرَحان** (مكتوبتان في `blocker` البندين):
`14:05Z→14:20Z` (جلسةٌ نظيرة · ~١١ `GET`) · `14:24:44Z→14:25:15Z` (مِجَسّي · ١٢ `GET`).
⚠️ **والمِجَسُّ يملأ الكاشَ الذي يقيس إصابتَه** ⇒ الحركةُ الصناعيّة **تُجمّل** النتيجة
⇒ إن تعذّر الطرحُ الدقيق فالرقمُ يُقرأ **حدّاً أعلى لا تقديراً**.

---

## ٣) 🔴 تصحيحٌ ذاتيّ — فرزٌ أعلنتُه ثمّ نقضه قياسي

أعلنتُ `13:09Z` أن «السطحَ العامَّ للمدرسة برِيء» بناءً على **عيّنةٍ واحدة**. والردُّ **غيرُ
حتميّ** ⇒ العيّنةُ الواحدة ليست قياساً، وقد أصابت الوجهَ السليم مصادفةً.

**المِجَسُّ المصحَّح — ١٢ عيّنة · مضيفان × ٦ · `14:24:44Z→14:25:15Z`:** الإخفاقُ **٢ من ١٢
(~١٧٪)، متطابقٌ على المضيفين** (‏5/1 لكلٍّ). الفارقُ بالبايتات **210,198** مقابل **208,818**،
و`<title>` يقلب إلى «يمن سكولز».

**ونُفيت فرضيّتان لجلسةٍ نظيرة:** ① «الـslug بلا حقنٍ أصلاً» — بل متقطّعٌ كالمضيف الآخر.
② «`Age: 0` هو السبب» — إخفاقٌ وقع عند `Age: 261`، **والحقلان مستقلّان بنيويّاً**: `Age` يصف
جلبَ HTML من Pages والحقنُ يقع بعده في الوركر.

**الآليّة:** `school-app-proxy.js:2330` ⇒ `_brandFromCache` تقرأ كاشَ الحافّة وحده، وعند
الإخفاق **تُخدَم الصفحة بلا هوية** (‏`fail-open` مقصودٌ ومكتوبٌ في `:2365`). وكاشُ الحافّة
لكلّ colo وأفضلُ جهدٍ لا ضمان.
🔴 **ولا يُصلَح قبل قياسه على حركةٍ حقيقيّة** — والمِجَسُّ لا يقيسه (يملأ ما يقيس)، فيلزم
حقلٌ يميّز `brand: hit|miss` عند `:2330`. **نفسُ درسِ ب‑١ حرفياً: الأداةُ قبل القياس.**

---

## ٤) 🔴 عطلٌ حيٌّ حُسم جذرُه — والتنفيذُ في مستودعٍ آخر

**«الدخول بالبصمة لا يعمل — الجهاز غير مسجَّل أو أُلغي»**، وجُرّب فلم يُصلَح.

**الجذرُ مقيسٌ من طرفَي المقارنة نصّاً:** التسجيلُ يكتب `session.schoolId || ''` وهي **`''`
لجلسة المالك** (‏`_Tenant.js:208` يقولها نصّاً)؛ والدخولُ يقارنها بقيمةٍ **من الصفحة** لا
تكون فارغةً أبداً — `frontend/teacher/index.html:41` ينتهي بـ**UUID مثبَّتٍ في الكود**.
⇒ `'' !== '12725ed7-…'` ⇒ **فشلٌ حتميٌّ للمالك وحده**، و**إعادةُ التفعيل لا تُصلحه**.

📄 التفصيلُ والرقعةُ المقترَحة والضابطُ المعاكس: بند
`biometric-login-broken-for-owner-schoolid-mismatch` · وذاكرةُ المشروع
`owner-schoolid-empty-breaks-identity-matching`. **التنفيذُ في `SchoolApp-gas` بموافقة
مالكها** — سُلّمت الحزمةُ كاملة.

---

## ٥) التنسيق — ستُّ جلسات · وما مُنع أهمُّ ممّا نُفِّذ

**ستّةُ فروضٍ فُحصت اليوم بين الجلسات: أربعةٌ سقطت، وواحدٌ انتقل مرّتين، وواحدٌ تأكّدت بنيتُه.**
🎯 **وصفرٌ منها تحوّل إلى تعديلٍ في ملفٍّ حيّ.** ولو مُسّ `assetlinks.json` على الفرض الأوّل
(‏علاقةُ اعتمادٍ لـWebAuthn) **لانكسرت App Links لمئات التثبيتات بلا أن يحمرّ أيُّ بناء** —
والذي منعه أن **كلَّ جلسةٍ رفضت تصديقَ الأخرى**، وأن اسمَ العلاقة **لم يُتحقَّق منه** بعد
ستّ محاولاتٍ من جلستين. ⇒ **صفرُ تعديل.**

**قواعدُ تنسيقٍ ضُيِّقت بالقياس:**
· **الدمجُ في `SchoolApp-gas` يَنشُر الوركر فقط حين تتغيّر مخرَجاتُ `frontend` فعلاً** —
  خمسُ دمجاتٍ في يومٍ واحد أنتجت **بناءً واحداً**. ⚠️ والاحتياطُ يبقى: يُفترَض النشرُ حتى
  يُقاس عدمُه.
· **أسألُ عن دمجٍ حين يوجد مسارُ نشرٍ آليٌّ إلى سطحٍ أقيسه — لا عن كلّ دمج.** مستودعا
  الأندرويد **لا يشحنان بالدمج**؛ القناةُ إصدارٌ يصل `production`.
· 🔴 **و`tracks.get` يقول `completed` والتوزيعُ لم يبدأ** — النشرُ المُدار يحجبه حتى يضغط
  المالك. ⇒ **ضابطٌ آليٌّ مبنيٌّ عليه يُعلن الوصولَ وهو لم يقع.**

---

## ٦) ما ينتظر المالكَ — ولا يتقدّم بدونه

| # | البند | الحال |
|---|---|---|
| ١ | **ضغطُ «نشر التغييرات»** لـ`vc34` | معيَّنٌ على `production` منذ `14:02Z` · **صفرُ جهازٍ استلمه** · بند `vc34-managed-publish-button-pending` |
| ٢ | **ضابطُ البصمة المجّانيّ** — الدخولُ من `?school=<UUID>` مقابل الرابط العميق | اختلافُ النتيجة **برهانٌ فوريّ** على §٤ |
| ٣ | إقرارُ وسم `[data-brand-href]` في جلسة `SchoolApp-gas` | القرارُ صدر هنا؛ **العائقُ موافقتُه هناك** — لا يُنقَل إذنٌ عبر الجلسات |
| ٤ | **تجربةُ فصل نشرة الطالب** | 🔴 **التوصية: لا تُنفَّذ** — سؤالُها أُجيب: الحوضُ مشتركٌ على مستوى الحساب (‏`p = 0.015`)، ونشرةٌ ثانية **لا تزيد الحصّة**. والمقدّمةُ الأخيرة أُغلقت بمِجَسٍّ على النشرة الحيّة `13:20:17Z` |

---

## ٧) ما لم يُقَس — يُكتب صراحةً

1. ~~**`uniq(k)`** — الأداةُ نُشرت، والرقمُ لم ينضج.~~ 🟢 **قِيس في اليوم نفسِه —
   الجدولُ في §٢.** ويبقى منه **غيرُ مقيسٍ حقّاً**: البراندان عند `uniq = 1` والضابطُ
   الوهميّ `1` أيضاً ⇒ القياسُ عاجزٌ عن التمييز هناك بنيويّاً؛ يلزمهما مستأجرٌ ثانٍ.
2. **سببُ تحسّن نسبة إصابة الكاش** (‏٤٨٪ مقابل حكمٍ قديمٍ ٠٫٥٨) — **مجهول**، ولا يُنسَب إلى تدخّل.
3. **معدّلُ تقطّع حقن الهوية عبر اليوم** — ١٢ عيّنةً من **مخرجٍ واحد** في ٣١ ثانية؛ لا يُعمَّم.
4. **وميضُ الهوية بعد الرسم** — يلزمه متصفّح.
5. **`session.schoolId` للمالك حيّاً** — البنيةُ مؤكَّدةٌ نصّاً، والتحقّقُ الحيُّ لم يقع.
6. **نداءاتُ GAS→GAS** لا تمرّ بهذا الوسيط ⇒ غيرُ مرئيّةٍ لسجلّاته.
