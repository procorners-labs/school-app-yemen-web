# قراءةُ ذروة 2026-09-18 (05–11Z) · وخطُّ أساسِ الأحد 09-13 قبل انقضائه

> جلسة `schoolapp-c8` · قِيس 2026-09-18 ~19:00Z · بند `worker-load-relief-after-measure`.
> المصادر: Workers Observability (`ev:gas` · `ev:apicache`) + `gcloud logging read` على `school-494822`.
> 🔴 **يُتتبَّع لأنه لا يُعاد إنتاجُه:** احتفاظُ Observability سبعةُ أيام ⇒ أرقامُ 09-13 تنقضي ~2026-09-20T05Z.

## ⚠️ الحكمُ الأوّل: ذروةُ اليوم ليست ذروة

**2026-09-18 جمعة — عطلةُ نهاية الأسبوع في اليمن.** نداءاتُ GAS في النافذة **230** مقابل **383** للخميس و**720** للأحد.
⇒ **مقارنةُ الجمعة بالخميس لا تُنسب أثراً لأيّ PR** — الحجمُ والمزيجُ مختلفان (حملُ الدرجات `autoSaveGradesBatchProtected`/`saveGradesProtected` = 41 الخميس و0 الجمعة).
⇒ **المقارنةُ الصالحة: الأحد 2026-09-20 05–11Z مقابل الأحد 2026-09-13 05–11Z** (أدناه — كلاهما يومُ دراسة، والثاني قبل #297/#298/#300 و#1595/#1596).

## `ev:gas` — حسب `ok · why` (05–11Z)

| اليوم | n | ok | abort_budget | upstream_status | upstream_html | فشل |
|---|---|---|---|---|---|---|
| **الأحد 09-13** (خطُّ الأساس) | **720** | 548 | 163 | 0 | 9 | **23.9%** |
| الخميس 09-17 (قبل #297 — دُمج 14:12Z) | 383 | 248 | 130 | 4 | 1 | 35.2% |
| الجمعة 09-18 (بعد الكلّ — عطلة) | 230 | 133 | 90 | 7 | 0 | 42.2% |

## لكلّ دالّة — ما يخصّ التسليم

| الدالّة | الأحد 09-13 | الخميس 09-17 | الجمعة 09-18 |
|---|---|---|---|
| `checkAppVersion` نداءاتُ GAS | **38** (ok 28) | 14 (ok 11) | **6** (ok 4) |
| `apicache checkAppVersion` | — | — | hit 5 · store 4 |
| `studentHeartbeat` (#1596) | ok 40 · fail 7 | ok 17 · fail 6 | ok 5 · fail 3 |
| `getStudentBootBundle` | ok 32 · fail 31 | ok 15 · fail 19 | ok 6 · fail 13 |
| `getTeacherBootBundle` | ok 8 · fail 3 | fail 2 | ok 3 · fail 3 |

## `ev:apicache` — `nokey` هبط (البند ②)

| | الأحد 09-13 | الخميس 09-17 | الجمعة 09-18 |
|---|---|---|---|
| `nokey` (`getHomeScheduleBundle` + `getStudentSchoolBrand`) | 0 | **37** | **1** (+2 `getTeacherSchoolBrand`) |

⚠️ صفرُ الأحد لا يعني «سليم»: قاعدةُ `nokey` قد تكون أحدثَ منه — لم يُتحقَّق من تاريخ دخولها. والمقارنةُ الصالحةُ هنا الخميس⇒الجمعة، **مع حدّ الحجم أعلاه**.

## gcloud — `[BOOT-COST]` من #1595 (ms)

مرشّح `resource.type="app_script_function" AND jsonPayload.message:"BOOT-COST"` ·
198 سطراً من 2026-09-17T17:47Z إلى 09-18T18:53Z.
🔴 **فخٌّ وقع:** `textPayload:"BOOT-COST"` ⇒ **صفرٌ كاذب** — الرسالةُ في `jsonPayload.message`.

| المكوّن | p50 | p95 | max | فوق 20ث |
|---|---|---|---|---|
| **teacher** (n=37) — `myPointsHeader` | **15,756** | 35,887 | **94,795** | 12 |
| teacher — `notifCounts` | 5,263 | 9,014 | 9,464 | 0 |
| teacher — `bootConfig` | 709 | 1,426 | 1,960 | 0 |
| teacher — `total` | 20,151 | 44,078 | 103,201 | 19 |
| **student** (n=161) — `reports` | 92 | **19,483** | 29,205 | 4 |
| student — `badge` | 2,295 | 5,563 | 7,124 | 0 |
| student — `total` | 3,663 | 26,570 | 36,972 | 58 |

🎯 **`myPointsHeader` هو مستهلكُ إقلاعِ المعلّم** — وسيطُه وحدَه 15.7ث من ميزانيةٍ قدرُها 24ث.
وعند الطالب **`reports` ثنائيُّ النمط**: وسيطٌ 92ms (كاش) وذيلٌ ~19ث (قراءةٌ باردة).
⚠️ n صغيرٌ للمعلّم في نافذة الجمعة (6) ⇒ الأرقامُ أعلاه للنافذة كاملةً لا للذروة.

## نافذةُ المعلّم (d3 — لوحةُ المعلّم عند الطلب)

`ev:gas · app=teacher` · **04:20→19:10Z** يوماً بيوم (الخميس 09-17 قبل · الجمعة 09-18 بعد).
أُغلقت النافذةُ عند 19:10Z بقرار المالك (نشرُ المعلَّق) بدل 04:20Z غداً.
🎯 **المقياسُ نسبةٌ لا عدد** — نداءاتُ اللوحة لكلّ إقلاع — لأن الجمعة عطلةٌ فالحجمُ لا يُقارَن.

| | الخميس (قبل) | الجمعة (بعد) |
|---|---|---|
| `getTeacherBootBundle` (إقلاع) | 23 | 16 |
| `handleTeacherLogin` | 25 | 34 |
| نداءاتُ اللوحة: `getSchoolCalendar` + `getMyDaySchedule` + `getMyTasks` + `getMyActivities` | **25** (6+6+7+6) | **1** (1+0+0+0) |
| **نداءاتُ اللوحة لكلّ إقلاع** | **1.09** | **0.06** (‏−94%) |
| `getOnlineNowProtected` | 14 | 4 |
| `getTeacherAbsenceSummaryProtected` | 10 | 1 |

🟢 **d3 يعمل:** الدخولُ لم يقلّ (34 مقابل 25) ونداءاتُ اللوحة شبهُ اختفت ⇒ الحجمُ لا يفسّر الهبوط.

🔴 **وما لم يتحسّن ويجب أن يُقال:**
- `getTeacherBootBundle` يفشل **11 من 16 (69%)** مقابل 17 من 23 (74%) — الإقلاعُ نفسُه ما زال فاشلاً، ومستهلكُه `myPointsHeader` (أعلاه).
- `handleTeacherLogin` يفشل **20 من 34 (59%)** مقابل 9 من 25 (36%) — **ارتفاع**. قد يكون الحِملَ المسائيَّ للدرجات (`autoSaveGradesBatchProtected` = 110 الجمعة) — **والعشرون كلُّها `why=abort_budget`** (صفرُ `upstream_status`/`upstream_html`) ⇒ إشباعُ حصّةٍ لا خطأُ كود، ولا صلةَ له بـd3 الذي لا يمسّ الدخول.

## ما بقي

1. **الأحد 2026-09-20 05–11Z:** تُعاد الاستعلاماتُ الثلاثة حرفياً وتُقارَن بصفّ الأحد 09-13 أعلاه.
2. `myPointsHeader` مرشّحُ تحسينٍ في `SchoolApp-gas` — قرارُ نطاقٍ لجلسةِ هناك، لم يُمَسّ هنا.
