# سجلُّ تنفيذ الوركر — ما لا يسجّله `git log`

🔴 **ولماذا وُجد، والعلّةُ مقيسةٌ لا مفترَضة (2026-09-10):**
**كلُّ دمجٍ في `main` هنا نشرٌ حيٌّ للوركر** — و`git log` يسجّل **الكوميت** ولا يسجّل:
① **هل نُشر فعلاً** (‏بناءُ Cloudflare قد يفشل بعد دمجٍ ناجح) · ② **ماذا قِيس حيّاً بعده** ·
③ **وما القرارُ الذي أُخذ ولم يُنفَّذ ولماذا**.
⇒ **وقِيس في 2026-09-10 أن هذا المستودعَ بلا أيّ سجلِّ تنفيذٍ رغم ٣٤ وثيقةً في `_docs/`
و١٦ دفعةً في يومٍ واحد** ⇒ **الأثرُ التشغيليُّ يُستخرَج من `git log` وحده، وهو لا يعرف النشر.**
⚠️ **وسجلُّ الجذر (`~/docs/changelog-workspace.md`) لا يغطّي هذا** — آخرُ مدخلٍ فيه
**2026-08-29** (‏متوقّفٌ ١٢ يوماً عند إنشاء هذا الملفّ)، وهو **يخصّ مستودعَ الجذر لا الوركر**.

## 🔴 قواعدُ هذا السجلّ — تُقرأ قبل الإضافة

1. **لا يُنسَخ عنوانُ كوميت.** `git log` يفعل ذلك أحسنَ منه. **هذا السجلُّ للأثر لا للتغيير.**
2. **كلُّ مدخلٍ يحمل طابعَ نشرٍ من سجلّ بناء Cloudflare** — 🔴 **لا من `workers_list.modified_on`**
   (ليس طابعَ نشر) **ولا من وقت الكوميت** (يسبق النشر بثوانٍ أو لا يُنتجه أصلاً).
3. **وما قِيس حيّاً بعده يُكتب بالأمر الذي قِيس به** — وإلّا صار المدخلُ ادّعاءً.
4. **ويُولَّد الأساسُ لا يُكتب يدوياً:**
   `git log origin/main --since=<تاريخ> --format="%h|%cI|%s"`
5. 🔴 **والقرارُ الذي لم يُنفَّذ يُسجَّل مع سببه** — البنودُ في `pending.json`، **والسجلُّ يُحيل
   إليها بمعرّفها ولا يعيد سردَها** (‏وإلّا تباعد الاثنان).

---

## 📌 2026-09-09 → 2026-09-10 — يومُ التقاعد والحرّاس

**١٦ دفعةً على `origin/main`** (‏١٣ منها PRs مندمجة · ودفعةُ `frontend` آليّةٌ واحدة
`7212bb9` من `SchoolApp-gas@a81ff58`).
**النطاق:** `638e2c9` → `0caa5ed`.

### الأثرُ الدائم — بترتيب القيمة

| # | ما أُنجز | الأثرُ الذي لا يُقرأ من الكوميت |
|---|---|---|
| ① | **عقدُ كاش الحافّة** `worker/edge-cache-contract.json` + تأكيدٌ ثنائيُّ القطب | يُغلق **انكساراً صامتاً عبر المستودعين**: إعادةُ تسميةِ دالّةٍ في GAS كانت **تُبرّد الكاشَ بلا خطأٍ ولا ٥٠٢**. **ونصفُه الثاني بندٌ عند الخلفية** |
| ② | **حارسُ المسارات المجمَّدة في APK** — فئةٌ لا حالات | 🟢 **أمسك انتهاكاً حقيقياً ثلاثَ مرّات**: `schedule` (09-05) · `student` · `pricing` (09-10) — والثالثةُ **بعد ساعةٍ من كتابته** |
| ③ | **حذفُ `/pricing`** بترتيبٍ إلزاميّ | الروابطُ أوّلاً ثمّ المعالج — **وعكسُه كان يُنتج ٤٠٤ من الجذر**، لأن `home/schools.html` هو ما يُخدَم به `/` |
| ④ | **إعادةُ `x-frame-options`** | **دَينٌ أمنيٌّ كشفه الحذف**: مبرِّرُ إسقاطه كان `/pricing` داخل `<iframe>` ⇒ زال المبرِّرُ فعاد الرأس. **ولولاه بقي تدهورٌ بلا سببٍ ولا إشارةٍ حمراء** |
| ⑤ | **الصلاحيات:** `allow` ٩⇒٤٥ · `ask` ٠⇒١٨ · `deny` ٠⇒٤ | **تغطيةُ الأفعال الماحية `19/19`** بأداة `ask-coverage.js`. وأُغلقت ثقوبٌ: `git checkout *` (دهسُ عمل) · `gh pr *` (نشرٌ حيّ) · `git branch *` · و`git push -f` بدرجةٍ خاطئة |
| ⑥ | **إزالةُ `cloudflare-api__execute`** من `allow` | **مُنفِّذُ API عامٌّ على الحساب · صفرُ استعمالٍ اليوم** ⇒ سطحٌ واسعٌ بلا عائد. **وأثرُ الإزالة مطالبةٌ لا كسر** |

### طوابعُ النشر المقيسة (‏من سجلّ بناء Cloudflare)

| PR | كوميت | البناء | النتيجة |
|---|---|---|---|
| #226 | `8677256` | `b7076f84` · `21:07:16.785Z` | `success` |
| #227 | `e0687cb` | `8b5e2939` · `21:27:39.780Z` | `success` |
| #228 | `ae1db4c` | `ac89f03b` · `21:38:44.250Z` | `success` |
| #229 | `bcf6ee6` | `9b6ad57f` · `22:18:50.737Z` | `success` |
| #230–#238 | `e537026` … `0caa5ed` | فحوصُ `gh pr checks` خضراءُ لكلٍّ | `success` |

### التحقّقُ الحيُّ بعد آخر دفعة — بالأمر لا بالادّعاء

```bash
curl.exe -s -D - -o <ملفّ> --max-time 60 "https://yemenschoolz.com<المسار>"
```
| المسار | المقيس |
|---|---|
| `/` | `200` · 4,561ms |
| `/student/index.html` · `/schedule/index.html` · `/home-all-school/index.html` | **`200`** — عقدُ الـAPK |
| `/teacher/index.html` | `200` |
| **`/pricing`** | **`404`** — مقصود |
| `X-Frame-Options` | **`SAMEORIGIN`** |

🔴 **ولا `curl -w`** (يعيد `000` كاذباً هنا) · **ويُفحَص الملفُّ المحفوظ لا الرأسُ وحدَه**
(‏`-o /tmp/…` على ويندوز **يفشل صامتاً والرأسُ يطبع 200**).

### 🔴 وما لم يُنفَّذ — ولماذا (يُحيل إلى `pending.json` ولا يعيد سردَه)

| البند | المانع |
|---|---|
| `GAS.pricing` لم يُحذف | **حجبه `protect-deploy-ids`** — يقرأ **مجموعةَ** المعرّفات ولا يميّز الخاملَ من الحيّ. ونصُّه: قرارُ مالكٍ **يُنفَّذ بتعطيل الحارس لا بالالتفاف عليه** ⇒ **لم يُتجاوَز** |
| `'pricing': 1` بقي محجوزاً | إسقاطُه يجعل المسارَ **مرشَّحَ slug مدرسةٍ قابلاً للاختطاف** |
| CSP كاملة | `csp-absent-on-worker-responses` — سكربتاتُ الصفحات مضمَّنةٌ بكثافة ⇒ تكسرها صامتةً |
| ١٢ فرعاً محلّياً عالقاً | فعلٌ لا يُعكَس، وثلاثُ جلساتٍ تعمل |
| السطحان القانونيّان | `two-live-privacy-policies-diverged` — قرارٌ قانونيّ |

### وقياساتٌ على المنبع تخصّ الخلفية — سطحُ الوركر يراها ولا يملكها

**نافذتان من `ev:'gas'`** (‏قبل نشر الخلفية `17:00Z→21:05Z` وبعده `22:00Z→01:40Z`):
- **`teacher`: `avgGasMs` 13,375 ⇒ 13,377** — **فرقُ ٢ ملّي‑ثانية** ⇒ **صفرُ أثرٍ للنشر**.
- **`avgLen` لـ`teacher`: 15,888 ⇒ 1,274** ⇒ الحمولةُ **أخفُّ ١٢×** ⇒ فرضيّةُ «حمولةٌ أثقل» مُبطَلة.
- **`p95 = 23,700ms`** على `student`/`teacher`/`home` **قبل** النشر ⇒ **سقفُ الميزانية سابقٌ للعمل**.
- 🎯 **والحملُ انخفض ~١٠×** (‏`student` 1,556⇒144) **والزمنُ لم يتحرّك** ⇒ **البطءُ لا يتبع الحمل**
  ⇒ **مستهلكُ الوقت داخل التنفيذ** — **وهو حجّةُ الهجرة إلى Postgres، حُسمت بقياسٍ جديدٍ لا بأرقام أغسطس.**
- 🔴 **و`?action=health` وحدَه ٤/٤ عند السقف** ⇒ **المِجَسُّ أثقلُ نداءٍ في المنصّة** ⇒
  **لا يُستعمل مقياساً لصحّة المنصّة بعد اليوم**؛ ويبقى لغرضه الأصليّ (بند 21 عند الخلفية).

---

## 📌 2026-09-10 (‏فجراً) — دفعةُ الرؤية: إصلاحان في السجلّ كشفا أعطالاً حيّة

**النطاق:** `56152e0` → `303b3f3` · **٣ دفعاتٍ حيّة** (‏#239 · #240 · #241).

### 🎯 والقاعدةُ التي تلخّص الدفعة: إصلاحُ الإسناد لم يكن تحسيناً بل **شرطاً لرؤية العطل**

| # | الإصلاح | ما كشفه |
|---|---|---|
| ① | **`_` في نمط استخراج اسم الدالّة** (‏#240) | كان النمطُ `[A-Za-z][A-Za-z0-9]{0,63}` ⇒ **كلُّ دالّةٍ فيها شرطةٌ سفلية تُسجَّل بلا اسم**. والشاهدُ من سجلّ متصفّح المالك: `smm_loadPlan` و`smm_loadAccounts` تفشلان **وكلتاهما غائبةٌ عن إسنادنا** ⇒ 🎯 **العطلُ الأشدُّ ظهوراً للمستخدم كان الأخفى عنّا بنيويّاً** |
| ② | **حقلُ `why`** (‏#241) | كان التمييزُ بين «الوركرُ أجهض» و«المنبعُ أخطأ» **في نصّ الجسم العربيّ وحدَه** ⇒ **كلُّ ٥٠٢ سواءٌ في السجلّ** ⇒ **بُحث عن العطل في المنبع ثلاثَ مرّاتٍ والمنبعُ سليم** |

**وأطرافُ `why` تفرز ثلاثةَ مستودعاتٍ كانت برمزٍ واحد:**
`abort_budget` ⇒ زمنُ GAS أو ميزانيةُ الوركر · `upstream_status` ⇒ كودُ GAS ·
`upstream_html` ⇒ اعتراضُ صفحةٍ من Google · `transport` ⇒ الشبكة.
🟢 **والجسمُ لم يتغيّر بحرف** — لا يمسّ عميلاً ولا APK، **ويحرسه ضابطٌ معاكسٌ يُحمِّر على تسريبه.**

### ما ظهر فورَ عمل الإسناد — ولم يكن في أيّ بندٍ عند أيّ جلسة

| الدالّة | المقيس | الحكم |
|---|---|---|
| 🔴 **`getTeacherBootBundle`** | `abort_budget` عند `23,700` | **بابُ منصّة المعلّم** |
| 🔴 **`getStudentNews`** | **٢/٢ عند السقف** | **مؤكَّدٌ من سطحين**: تواترُ ٨٦٪ من سجلّ GAS + زمنٌ من سجلّي |
| 🔴 **`teacherHeartbeatProtected`** | ٢٠ نداءً · `avg 11,725` | **نبضةٌ دوريّةٌ تكلّف ١٢ث** ⇒ تُشبع بلا مستخدمٍ نشِط |
| 🔴 ثلاثُ تقاريرَ إداريّة | `abort_budget` × ٣ | **نمطٌ واحد: تجميعٌ على ورقةٍ كاملة** ⇒ علاجٌ واحد |
| ⚠️ `getStudentBootBundle` | `max 23,494` من سقفِ `23,700` | **٢٠٦ ملّي‑ثانيةٍ عن انفجارٍ جماعيّ** |
| 🟢 `checkSession` | `4,052ms` `ok` | **المصادقةُ تعمل** |

### 🔴 وأربعةُ أحكامٍ لي بطَلت في نفس اليوم — تُقرأ قبل الاعتماد على أيّ رقمٍ منّي

1. ~~«المنظّمُ يُشبع فيأكل الميزانية»~~ ⇒ **`avgQ = 0` و`n = 1–3` من ٨** ⇒ **المنظّمُ بريءٌ ثلاثَ مرّات.** 🟢 **ونقضَتها البياناتُ قبل أن أشحن إصلاحاً عليها.**
2. ~~«كلُّ ٥٠٢ أثرُ مِجَسّي»~~ ⇒ **٣٦ إخفاقاً بلا أيّ `?action=health`.** (‏و`health` مريضٌ بذاته: **٤/٤ عند السقف**.)
3. ~~«`getUnreadNotesCountProtected` معطوبةٌ دائماً»~~ ⇒ **نجحت في `6,677ms`** ⇒ **متذبذّبة.**
4. ~~«يدخل ثمّ لا تُحمَّل اللوحة»~~ ⇒ **لا يدخل أصلاً** — والجذرُ **مدرسةٌ بلا عمودِ بريدٍ** ورسالةٌ عمياء.

🎯 **والجامعُ: كلُّها تعميمٌ من عيّنةٍ جزئيّةٍ أو من مِجَسٍّ هو نفسُه الحالةُ الشاذّة.**
🟢 **والفارقُ أن الأربعةَ صُحّحت بقياسٍ قبل أن يُبنى عليها فعل** — **والرابعةُ صحّحتها جلسةُ الخلفية من سجلّها الخادميّ.**
🎯 **ودرسٌ مشترك: سجلُّ الحافّة يرى ما وصل · وسجلُّ المنبع يرى أيَّ فرعٍ نُفِّذ** ⇒ **وصل كلٌّ منّا إلى نصف الجواب** — وهذا **ازدواجُ قياسٍ يستحقّ ثمنَه**، بخلاف أربعةٍ عُدّت ثمناً بلا عائد.

### وقياسٌ يحسم مبرِّرَ الهجرة — بلا أرقامٍ منقولة

**الحملُ انخفض ~١٠×** (‏`student` 1,556 ⇒ 144 نداءً) **والمتوسّطُ ثابتٌ عند ~١٣٫٣ ثانية** ⇒
🔴 **البطءُ لا يتبع الحمل ⇒ مستهلكُ الوقت داخل التنفيذ.** ⇒ **لا تحسينَ وركرٍ يُنزل ذلك إلى
مئات الملّي‑ثوانٍ، والفهرسُ يفعل.** **الوركرُ يؤجّل الألمَ لا يُزيله.**

### وما لم يُنفَّذ — بمالكه

`master-admin` أربعةُ مداخلَ تردّ ٢٠٠ (‏`owner: user` — **و`register.html` وجهةُ روابطَ حيّةٍ فلا يُعامَل مع `index.html`**) · إطالةُ الميزانية (‏`owner: user` — **بثلاثة حدود**) · وضابطُ كبح `registerDeviceToken` **ينتظر نافذةً نهاريّة** (‏خطُّ الأساس المثبَّت: **٧٫٩/ساعة**).

---

📄 التفصيل: `2026-09-10-عقد-الوركر-أمام-هجرة-Supabase.md` ·
`2026-09-10-تقاعد-المشاريع-الأربعة-وازدواج-المستودعات.md` ·
`2026-09-10-صلاحيات-مستودع-الوركر.md` · `2026-09-10-ذيل-تسليم-جلسة-الوركر.md`.
