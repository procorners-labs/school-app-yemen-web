# تسليمُ جلسة الوركر — حذفُ `worktrees` · CSP الإقليميّ · وتقاعدُ المشاريع

**الجلسة:** `freeze-worker-measurements` · **المستودع:** `SchoolApp` (الوركر)
**التاريخ:** 2026-09-11 · **`origin/main` استقرّ على `4b740f7`**

> 🔴 **اسمُ الملفّ يحمل تمييزاً لا التاريخَ وحدَه** — جلستان في يومٍ واحدٍ على نفس الجذر
> ليست حالةً نادرة، وقد وقعت. (بندُ «الشجرةُ النظيفةُ لا تحمي من دهسِ الملتزَم».)
>
> 🔗 **والطرفُ المقابل:** تسليمُ جلسة `SchoolApp-gas` في `_planning/` بذلك المستودع —
> **يُحال إليه ولا يُنسَخ منه.** 🔴 **ولا رقمَ منقول:** وقعت فئةُ «النقلُ عن نظيرٍ ليس
> قياساً» بيننا **مرّتين في هذه الجلسة وكلُّ الأرقام صحيحة** (‏٩٬١٠٩ مقابل ٧٬٥٦٣ ·
> و٩٣٬٣٨٠ مقابل ٩٢٬١٢١) — **والفارقُ سطحُ القياس لا الحقيقة.** ⇒ **كلُّ رقمٍ هنا مقيسٌ من
> هذا المستودع، ومعه أداتُه كي يُعاد قياسُه لا ليُصدَّق.**

---

## ١) ما قِيس — ومعه أداتُه

| ما قِيس | الأداة | النتيجة |
|---|---|---|
| تقاعدُ المشاريع في المصدر | `git ls-tree -r origin/main --name-only -- <د>/` على `SchoolApp-gas` | `schedule/` **٠** · `student/` **٠** · `pricing/` **٠** · `home-all-school/` **٩** |
| حجمُ الصفحتين | `git cat-file -s origin/main:<ملفّ>` | `home/Schools.html` **٩٢٬١٢١** · `home-all-school/Index.html` **١٢٦٬٢٧٨** ⇒ **+٣٤ ك.ب** |
| مخالفاتُ CSP الإقليميّة | `query_worker_observability` · `needle` · نافذة 09-10→11 | `region1` **٣ أحداثٍ فأكثر** · `region2` **صفر** · نصٌّ مختلَق **صفر** |
| بوّابةُ الوركر | `node --check` · `node worker/test-routes.js` | `0` · `0` (‏**٥٠٧ فحصاً** · `RESULT: ✅ 40 مساراً`) |
| حذفُ `worktrees` | `ls -1A .claude/worktrees/` · `find <أرشيف> -type f` | **صفرُ عنصر** · الأرشيفُ **٣٨٠ ملفّاً** |

🔴 **وضابطٌ ثنائيُّ القطب على مِجَسِّ السجلّات — وبدونه كان الصفرُ سيُقرأ عطلاً:** المُنظِّرُ
كان يسقط بخطأِ نوعٍ على الأحداث الراجعة، **فالخطأُ نفسُه دليلُ وجودِ بيانات**؛ والنصُّ
المختلَقُ ردَّ `[]` نظيفةً ⇒ **المِجَسُّ يفرز، فصفرُ `region2` صادقٌ لا عُطل.**
🎯 **و`region2 = صفر` يسند الحجّةَ ولا ينقضها:** التعدادُ كان سيضيف `region1` وحدَه
**فيُخالَف بأوّل زائرٍ من إقليمٍ ثانٍ** — وهي فئةُ «قائمةٌ تُعدِّد ما رأيناه لا ما يمكن أن يقع».

⚠️ **وفرقُ الأرقام مع الجار مُفسَّرٌ بالطرح لا بالترجيح:** `93,380 − 92,121 = 1,259` **وهو
عددُ الأسطر بالضبط** ⇒ **بايتُ `\r` لكلّ سطر**: هو قاس من **شجرة العمل** (CRLF) وأنا من
**كائنات git** (LF). وكذلك عدُّ `h1/h2` (‏١٢/٢٠ عندي · ١٠/١٨ عنده) **فرقُ نمطٍ لا محتوى** —
**والفارقُ بينهما ٨ في القياسين معاً**، وهو الرقمُ الحاكم.

---

## ٢) ما دُمج

| PR | المحتوى | الكوميت |
|---|---|---|
| **#262** | `connect-src` بنمطٍ لا باسم + قطبٌ خامسٌ في حارس CSP | `6093d7a` |
| **#263** | تصحيحُ وثيقة `worktrees` بعد تمام الحذف | `57883ae` |

**ضُمّا في دفعةٍ واحدةٍ عمداً** (‏`23541a4` ثمّ `4b740f7`) — **كلُّ دمجٍ هنا نشرٌ حيٌّ
يصفّر حالةَ المنظّم داخل الـisolate ولو كان المدموجُ وثيقةً محضة** ⇒ المنفصلان نشرتان
وبدءان باردان. **والاثنان `MERGED`.**

---

## ٣) 🔴 النشر — ثلاثةُ حقولٍ لا حقلٌ واحد

| الحقل | الحال |
|---|---|
| **ما دُمج** | `#262` · `#263` ⇒ `origin/main@4b740f7` |
| 🟢 **ما نُشر ووقتُه** | **نُشر ومُتحقَّقٌ منه حيّاً** — `school.procorners.com/teacher/index.html` ⇒ `HTTP/1.1 200 OK`، ورأسُ `Content-Security-Policy-Report-Only` **المخدوم** يحمل `https://*.analytics.google.com` و`https://*.google-analytics.com`. **الطابعُ من رأس `Date` الخاصّ بالردّ: `Fri, 11 Sep 2026 19:19:49 GMT`.** |
| 🟢 **مدموجٌ غيرُ منشور** | **لا شيء** |

🔴 **والفحصُ حيٌّ عمداً لا من خضرةِ بوّابة** — «نجح CI» ≠ «نُشر»، **ولسببين لا سبب**
(فرقُ `frontend/` الفارغ · وتشغيلةُ `workflow_dispatch` التي تتخطّى خطوةَ الدفع كلَّها).
**والصيغةُ المستعملة:** `curl.exe -s -D - -o /dev/null --max-time 60` **بلا راية `-w`**
(تعطي `000` كاذباً على هذا الجهاز)، ومع معامل كسرِ كاش.

⚠️ **وحركةُ رأسٍ قُرئت بمحتواها لا بمجرّدها:** `origin/main` تحرّك أثناء العمل
(`0d15e02 ⇒ 3ca955d`)، **والمقيسُ أنها دفعةُ `frontend` آليّة تمسّ
`frontend/master-admin/index.html` وحدَها** من كوميت GAS ‏`99b207b` — **وكانت جلسةُ
`SchoolApp-gas` قد أنذرت بها مسبقاً** ⇒ **لم تُقرأ نشرَ وركرٍ مجهولَ السبب.**
🎯 **والدرس: المؤشّرُ لا يُقرأ بدل الأثر** — حركةُ الرأس ليست حدثاً يُفترَض.

---

## ٤) ما بقي مفتوحاً — بنداً بنداً ومع سببِ بقائه

| البند | المالك | سببُ بقائه |
|---|---|---|
| `retired-columns-wording-invites-deletion` | **user** | 🔒 **ينتظر قرارَ المالك** — `CLAUDE.md` يحكم كلَّ جلسةٍ تُفتح هنا، **والطلبُ ورد من نظيرة** ⇒ **لا تُعدَّل تعليماتُ جلسةٍ بطلبِ نظيرة** ولو كان الطلبُ مقيساً، وهو كذلك. عُرض ولم يصل جواب |
| `frozen-apk-paths-must-stay-200` | claude | **قاعدةٌ تُطبَّق لا عملٌ ينتظر** — `blocker` فارغٌ صراحةً |
| `worker-frozen-through-supabase-phase6` | claude | قاعدةُ تجميدٍ سارية |
| `csp-absent-on-worker-responses` | claude | **دَينٌ مُعلَن**: السياسةُ `Report-Only` عمداً؛ النافذةُ تكسر صفحاتٍ سكربتاتُها مضمَّنةٌ بكثافة |
| `app-subdomain-cache-share-unmeasured-pre-switch` | claude | لم يُبدأ |
| `rotate-cf-account-id` · `worker-budget-vs-client-timeout-36s-unused` | **user** | فعلٌ لا تستطيعه الجلسة |

**وأُغلق في هذه الجلسة بنتائجَ مقيسة:** `dead-worktrees-poison-grep` ·
`home-all-school-source-retirement-open`.

---

## ٥) 🔒 ثلاثةُ بنودٍ مشتركةٍ مع `SchoolApp-gas` — تُكتب بحرفها كي لا تُقرأ عند طرفٍ وحدَه

**① حدُّ الطبقات الثلاث** — وهو ما يمنع كارثةً تبدو تنظيفاً:
**مصدرٌ _قد_ يُحذف · نشرةٌ تبقى · ومسارٌ مخدومٌ يبقى `200`.**
🎯 **ونقطةُ الانهيار غيرُ بديهيّة:** **لا فرعَ في الوركر يذكر هذه المسارات**، وبقاءُ `200`
يتّكئ **حصراً** على وجود الاسم في `_RESERVED_TOP_PATHS` — **وحذفُ الاسم أوّلُ ما يبدو
تنظيفاً بريئاً.** ومُثبَتٌ بالطفرة: حذفُ `'home-all-school': 1` ⇒ `RESULT: ❌ 2 فشل ·
EXIT=1` · والاستعادةُ ⇒ `✅ 40 مساراً · EXIT=0`.
🔴 **والأثرُ لا يُعكَس:** `SchoolAppyemen` منشورٌ على Play بـ`vc31`، و`syncIfNeeded()`
**معطَّلةٌ بالتعليق**، و`isValidUrl` ترفض كلَّ بديل ⇒ **من لا يُحدّث يبقى مكسوراً أبداً.**

**② `home-all-school/` مصدرٌ مقصودُ البقاء — وف3-د مُلغاة.** الملفُّ **هجينٌ بلا توأم**:
**ثمانيةُ أقسامٍ بلا نظير** (أرقامٌ تتحدّث عنّا · عن المدرسة · المراحل · لماذا مدرستنا ·
أخبار · ألبوم · فيديوهات · تواصل) — **والرقمُ ٨ صامدٌ على قياسَي المستودعين معاً** رغم
اختلاف البايتات. ⇒ **حذفُه خسارةُ محتوىً لا نقلُ كود**، على `DEFAULT_HOME` لتطبيق
`YemenSchoolz`. **وحدُّ المحتوى في `worker/test-routes.js` هو ما يمنع عودةَ الفكرة قبل أن
تصل المستخدم** ⇒ **يبقى.**

**③ صيغةُ الأعمدة المتقاعدة — نصفُها مطبَّقٌ ونصفُها بندٌ مفتوح.**
**«فارغُ القيمة · محجوزُ الموضع — لا يُحذف العمود»** طُبِّقت عند الجار
(‏`SchoolApp-gas@main 26acc0e`)، **والطرفُ المقابلُ هنا لم يُطبَّق** ⇒ مسجَّلٌ بنداً
بمانعه لا سطراً في سرد (البندُ في §٤). **وتسري على عمودين لا واحد**: `student_file_id`(٥)
**و**`schedule_file_id`(٧) — وإدراجُ الأوّل «مفتاحياً» في `CLAUDE.md:65` بائتٌ أيضاً.

---

## ٦) المطالبةُ التالية — نصٌّ جاهزٌ للّصق

```
اقرأ _docs/2026-09-11-تسليم-جلسة-الوركر-حذف-worktrees-وCSP-الإقليمي.md ثمّ:
(١) اعرض عليّ صيغة «فارغُ القيمة · محجوزُ الموضع» لسطرَي CLAUDE.md:65-67 قبل تحريرها —
    وتسري على student_file_id(5) وschedule_file_id(7) معاً لا على schedule وحدَه.
(٢) أعِد قياس مخالفات CSP الإقليمية بعد أسبوعٍ من النشر: هل ظهر region2 أو سواه؟
    والمتوقَّع صفرُ مخالفةٍ من العائلتين — فإن ظهرت فالبدلُ لا يعمل كما افتُرض.
(٣) لا تلمس _RESERVED_TOP_PATHS ولا تحذف اسماً منها قبل قراءة §٥① أعلاه.
```

🔴 **ولا يُقاس أثرُ ‏#262 في نافذةٍ تعبر دمجاً** — النشرُ يصفّر حالةَ المنظّم، فالنافذةُ
التي تعبره تحمل متغيّرين.
