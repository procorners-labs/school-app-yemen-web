# تسليم — تخفيف حمل GAS من الوركر (البنود ① · ② · ③)

> جلسة `schoolapp-cc` · 2026-09-17 · بطلب المالك عبر جلسة `schoolappyemen-d2`.
> المصدر المقيس للطلب: `SchoolAppyemen/_docs/2026-09-17-صحة-الخادم-بعد-نشر-vc36.md` §②–⑤.

## ما نُشر — بموافقة المالك اللحظيّة

| البند | PR | الدمج (Z) | البناء |
|---|---|---|---|
| ① `checkAppVersion` من كاش الحافّة (ttl 3600، `tenantless`) | #297 | `2026-09-17T14:12:05Z` | `febf296b` success |
| ③ `getNewsOg` لزواحف المعاينة وحدَها (`_isPreviewCrawler`) | #298 | `2026-09-17T14:13:44Z` | `7ca095bb` success |

صفرُ تغييرٍ في مسارٍ أو مضيف. `node --check` و`node worker/test-routes.js` أخضران، ولكلّ بندٍ طفرةٌ أحمرّت ثمّ أُعيدت.

## التحقّق الحيّ بعد النشر

**①** ثلاثة نداءات `POST https://yemenschoolz.com/gas/teacher` متتالية:
الأوّل 502 «مزدحمة» (25,057ms — قبل التخزين)، الثاني 200 `latestVersionCode 36` (5,467ms)، الثالث 200 **بنفس `_ms:143` حرفياً** (733ms).
والسجلّ (14:14→14:40Z): `apicache checkAppVersion store ×1 · hit ×1` ⇒ **الإصابة وقعت فعلاً**.

**③** `/home/newsarticle.html?news=<معرّفٌ وهميّ>` بترتيبين متعاكسين:

| UA | ms |
|---|---|
| WhatsApp | 7,131 |
| Chrome Android | 1,034 |
| Safari iPhone | 816 |
| facebookexternalhit | 4,487 |

الزاحفُ ينتظر GAS والمتصفّحُ لا — في الترتيبين ⇒ ليس أثرَ ترتيب.
⚠️ حدٌّ: معرّفٌ وهميّ ⇒ لم يُقَس حقنُ وسومٍ حقيقيّة بخبرٍ موجود؛ المقيسُ هو **وقوعُ الانتظار أو غيابُه**.

## البند ② — تفكيكُ أسباب الرفض (قراءةٌ فقط، لم يُعدَّل شيء)

`ev:apicache act:nokey` — 24 ساعة حتى 12:00Z، مجمَّعاً بـ`fn · why · app` (حقلٌ وهميٌّ ضابط ثابتٌ على 1):

| fn | why | app | n |
|---|---|---|---|
| `getHomeScheduleBundle` | sid | student | 101 |
| `getStudentSchoolBrand` | sid | student | 68 |
| `getHomePageBundle` | sid | home | 26 |

🎯 **صفرُ رفضٍ بسبب `args` — كلُّه `sid`:** الواجهةُ ترسل هذه النداءات **بلا معرّف مدرسة**.
⇒ **ليس نقصاً في الوركر**، وإرخاءُ القاعدة يخالف قرار المالك 2026-08-14. العلاجُ في مصدر الواجهة (`SchoolApp-gas`) — **ينتظر قرار المالك**.
و`listPartnerSchoolsPublic` لم تُضَف — بلا قياس.

## خطّ الأساس «قبل» — 24 ساعة `2026-09-16T14:12Z → 2026-09-17T14:12Z`

| المقياس | القيمة |
|---|---|
| `$metadata.trigger` يحوي `/gas/` — حسب `response.status` | 200 ×1774 · 502 ×725 · 503 ×96 · 0 ×3206 |
| `ev:gas` — حسب `ok · why` | ok ×1685 · abort_budget ×740 · upstream_status ×30 · upstream_html ×9 |
| `ev:apicache` (نافذة الوكيل، 24س حتى ~12Z) | store 221 · hit 96 · stale 15 · nokey 212 |

⚠️ **حدود المقارنة:** نافذةُ «قبل» تعبر دمجاتِ #293–#296 (ليست خاليةً من البناء). والرقمُ القادم من `schoolappyemen-d2` (200 ×4925 · 502 ×758) **بمقياسٍ آخر** — لا يُطرح من هذا.

## نافذةُ «بعد» — مفتوحة

- **من `2026-09-17T14:20Z` إلى `2026-09-18T14:20Z`.** تُعاد الاستعلاماتُ الثلاثة أعلاه حرفياً.
- مِجَسّي داخلها: 3 نداءات `checkAppVersion` + 4 طلبات صفحة خبر قبل 14:20Z ⇒ خارج النافذة.
- 🔴 **صمتُ الدمج:** `schoolapp-gas-61` تُبقي مفتوحةً: PR #1595 (خلفيّة + `clasp deploy` يغيّر الـ502 وحده — ستُعلن وقته بـZ) · والبند 6 (`sw.js` — فرقُ واجهة) · والبند 4 إن كان في HTML.
  ⇒ **لا يُدمج في هذا المستودع ولا يُنتَج فرقُ `frontend/` حتى يُعلَن إغلاقُ النافذة** — وهذه الوثيقةُ نفسُها تبقى PR مفتوحاً للسبب ذاته.

## ✅ قراءةُ الساعات الثلاث — لكلّ دالّة (أُغلقت النافذة 17:20Z بقرار المالك: الخيار A)

`14:20→17:20Z` اليوم مقابل النافذة نفسها أمس · `ev:gas` (أعلى ٦٠ صفّاً) · **«مساءً، حملٌ منخفض، لا يمثّل الذروة»**.

| المقياس | بعد | قبل (أمس) | الحكم |
|---|---|---|---|
| `checkAppVersion` نداءاتُ GAS | **10** (ok 7 · abort 3) | 47 (ok 31 · abort 16) | 🟢 **−79%** |
| `apicache checkAppVersion` | hit 18 · store 4 · skip 3 | — | 🟢 hit ≫ store |
| `getNewsOg` لغير الزواحف | **غيرُ قابلٍ للعدّ** — نداءُ OG لا يُسجَّل `ev:gas` | — | ⚠️ الإثباتُ مِجَسُّ UA الحيّ وحده |
| `/gas/` إجمالاً | 200×320 · 502×174 · 503×14 (فشل 37% · n=508) | 200×513 · 502×217 · 503×34 (33% · n=764) | ⚪ **ليس دليلاً** — حجمٌ ومزيجٌ مختلفان |

## ✅ PR #300 — «نافذةٌ واحدة» جانبُ الوركر

دُمج `2026-09-17T17:22:21Z` بموافقة المالك. تحقّقٌ حيّ (`window.__SCHOOL_BRAND__ =`، `curl --compressed` بمعاملٍ كاسرٍ للكاش):
`/teacher/abdaawatmuaz` ⇒ **1** · `/teacher/index.html?school=abdaawatmuaz` ⇒ 1 · الضابط `/teacher/visits` ⇒ **0**.
⚠️ حدّ: لم تُقَس حالةُ «قبل» لهذا المِجَسّ؛ الإثباتُ هو الضابطُ المعاكس واختباراتُ `_tenantKeyFrom`.
**قرارُ المالك (مؤكَّدٌ في هذه الجلسة):** الرابطُ الرسميّ `/teacher/index.html?school=<uuid>` و`/student/index.html?school=<uuid>`؛ التوحيدُ بـ`replaceState` في `SchoolApp-gas` بلا 30x ولا تغيير مضيف.

`schoolapp-gas-61`: نشرُ #1595/#1596 مخطَّطٌ `17:25Z` ⇒ **قراءةُ الذروة غداً 05–11Z لكلّ دالّة** تشمل #297 · #298 · #300 · #1595.

## ما بقي

1. ~~إعادةُ القياس بعد 24 ساعة~~ — استُبدل بقراءة 3 ساعات (أعلاه) + **قراءة الذروة 2026-09-18 05–11Z لكلّ دالّة**.
2. قرارُ المالك في البند ②: إصلاحُ إرسال المعرّف في الواجهة (`SchoolApp-gas`).
