# تسليمُ جلسة الوركر (ب) — 2026-09-26 · تنسيقُ دفعة «الطبيب» مع جلسة الخلفية

> الجلسةُ النظيرة: `SchoolApp-gas` (‏doctor-phase-v-performance-fixes)، وتسليمُها في
> `SchoolApp-gas/_planning/handoff-2026-09-26-*.md`. **لم يُعدَّل الوركرُ في هذه الجلسة إطلاقاً.**

## ما جرى

| الطابع (UTC) | الحدث | السطح |
|---|---|---|
| ~03:45 | `teacher@1004` — تقاريري: حزمةٌ مشتركة للشعبة (#1764) | GAS |
| ~04:12 | `teacher@1005` — الرصد: الوحدات من ترويسة «الدرجات» (#1765) | GAS |
| ~04:27 | `teacher@1006` — تقرير المعلّم من قارئ الشريط (#1766) | GAS |
| 04:43:18 | `teacher@1007` — معيار ④ في الطبيب · حذفُ `importStudents` (#1767) | GAS |
| 05:26:27 | `teacher@1008` — ⑦: الرصد يحلّ المواد من الترويسة المقروءة (#1768) | GAS |
| 06:02:52 | **#395 هنا** — واجهة ⑥ (عطلُ التقويم 🌴) · `03370c1` | Pages |
| 06:05:43 | `teacher@1009` — خادم ⑥ (#1769) | GAS |

الطوابعُ من جلسة الخلفية. و#395 **قِيس حيّاً** عند 06:03:55Z: علامتا ⑥ تُخدَمان على
`yemenschoolz.com` (الطالب ٣ · المعلّم ٤ = المستودع، و٠ قبل الدمج).

## مراجعةُ #395 (قبل الدمج)
ينحدر من `origin/main` (لا يدهس #391–#393) · ملفّان في `frontend/` وحده · `node --check` +
`test-routes` ⇒ 0 على رأس الـPR. **حُسم:** مادّةٌ حقيقيّةٌ في يوم عطلة **تُحفَظ مع تنبيه**
(`holidayConflicts`) لا تُرفض — قرارُ التنفيذ هناك. ومتابعةٌ مسجَّلةٌ هناك: يومٌ بلا تاريخ
يجمع صفوفَ العطل قبله (شكليّ).

## 🔴 الحقيقةُ التي تخصّ الوركر
**الطالبُ الأوّل البارد في «تقاريري» يستغرق ~25.5ث خادمياً** (`[SRPT-COST] totalMs=25518`،
`bundleMs=21540` — قِيس داخل GAS لا عبر الوركر) **وسقفُ POST عندنا 26000 ناقص الانتظار**
⇒ يُجهَض عند ~25,700 ويرى المستخدمُ خطأً/كاشاً قديماً، بينما يُكمل GAS ويملأ كاشَ الشعبة.
⇒ **العلاجُ خادميٌّ (⑧ — الجلسةُ التالية هناك)، ولا يُرفع السقف هنا** (قيدٌ مقيس).

## ⏳ ما بقي عليّ — القياسُ النهاريّ
النداءاتُ الليليّة التي قِستُها (01:35–03:29Z) **كلُّها قبل @1004** فلا تقيس شيئاً.
- `getStudentReports` بعد **04:43:18Z**، بثلاث فئات: `ms<5000` · `5000–25000` · `ms≥25000`
  (توزيعٌ ثنائيُّ النمط متوقَّع؛ الوسيطُ وحده يضلّل). **الفئةُ ≥25000 خطُّ أساس ⑧.**
- `getGradesEntryComplianceProtected` بعد **05:26:27Z** (متوقَّعٌ بارداً ~4ث — لم يُقَس).
- المصدر: `ev:'gas'` في `school-teacher-proxy` مجمَّعاً بـ`fn` و`ok` · وطابعُ #395/@1009 يقطع
  النافذةَ مرّةً ثالثة.
- **قبلُ (مرجعٌ للمقارنة، 09-23 05:00 → 09-25 21:00Z):** `getStudentReports` ‏110 نجاحاً /
  115 إجهاضاً · `median(gasMs|ok)=19,131` · `getGradesEntryComplianceProtected` ‏2 / 12.
