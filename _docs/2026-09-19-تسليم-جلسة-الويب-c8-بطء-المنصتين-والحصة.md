# تسليم جلسة الويب `schoolapp-c8` — بطءُ المنصّتين والحصّة المشتركة (2026-09-18 → 09-19)

> المالكُ طلب: قياسٌ بلا انتظار · نشرٌ منسَّقٌ مع جلسة الخلفية `schoolapp-gas-bc` · ثمّ تسليمٌ وإنهاء.
> 🔴 **كلُّ رقمٍ هنا لحظةَ قياسه** — ويُعاد قياسُه قبل البناء عليه (فئة بند 83-ب).

## ١) ما نُشر في هذا المستودع

| PR | ما يفعله | الدمج (Z) |
|---|---|---|
| #316 | وثيقةُ قراءة ذروة 09-18 + خطُّ أساس الأحد 09-13 + قياسُ لوحة المعلّم d3 | 09-18 19:03:35 |
| #317 | نقلٌ يدويّ لـ`frontend/teacher/index.html` (gas@69acc83 · teacher@942) | 09-18 19:18:43 |
| #318 | **روابطُ البوّابات تحمل المدرسة خادمياً** (`_PortalHref`، لا تنتظر كاش الهويّة) + `home/index.html` من gas@ff28dde | 09-19 11:48:32 |
| #319 | **حدُّ تسجيل المشاهدات العامّة**: 60 دفعة/دقيقة لكلّ IP قبل الكاش والمقعد، وردٌّ `ok:true` صامت | 09-19 13:21:40 |
| #320 | **هويّةُ المدرسة على الحافّة 3600ث** بدل 600 (قرارُ المالك) | 09-19 14:00:32 |

🔴 **CI الخلفية متوقّفٌ بحدّ دقائق Actions** ⇒ أيُّ فرقِ واجهةٍ من `SchoolApp-gas` يُنقَل يدوياً:
`git archive <sha>` في مجلّد مسودّات ⇒ `node _build/build-frontend.js` ⇒ مقارنةٌ بعد نزع `\r` ⇒ نقلُ الملفّ المختلف وحدَه.
⚠️ **و`sitemap.xml` يُستبعَد دائماً**: البناءُ من archive بلا تاريخ git يُسقط `lastmod` — أثرُ أداةٍ لا تغيير.

## ٢) ما نُشر في الخلفية (جلسة `schoolapp-gas-bc` — للسياق)
`teacher@942` → `@947` و`home@148`: إخراجُ «نقاطي» و«التقارير» من الإقلاع (`cacheOnly`) · تجميعُ «مدّة الاستخدام» في النبضة · فحصُ طلب التقييم من الكاش · عدّاداتُ الإشعارات وشارةُ الطالب من الكاش · قياسُ `[BOOT-COST]` و`[SAVE-COST]`. التفصيلُ في تسليمها بـ`SchoolApp-gas/_planning/`.

## ٣) الأحكامُ المقيسة — تُقرأ قبل أيّ قرارٍ لاحق

1. **الإقلاعُ خفّ داخل GAS:** المعلّم total p50 19.3ث ⇒ 8.5ث (n=6) · «نقاطي» 15.1ث ⇒ 0.24ث · تقاريرُ الطالب p90 18.5ث ⇒ 0.07ث.
2. **صفرُ فقدٍ في الحفظ التلقائيّ** (12:38–12:44Z): 10 نداءات عند الحافّة = 10 `SAVE-COST` ناجحة؛ الفاشلان عند الحافّة **مكتملان في GAS** ⇒ «حُفظت ولم تُؤكَّد». ⚠️ الحفظُ اليدويّ لم يكن مقيساً وقتَها (أُضيف `kind:'manual'` في teacher@947).
3. **مشغّلاتُ المتجر لم تعد تستنزف الحصّة**: ~33 د/يوم من 90 (جردُ «عمليات التنفيذ» 09-19). ⇒ **ذاكرةُ 09-17 (633 د/يوم) تاريخٌ لا حالة.**
4. **البطءُ الباقي زمنُ قبولٍ عند Google**: تزامنُ الوسيط ≤ 5 من 30 مع إجهاضٍ عند 23.7ث، ونشرةُ `teacher` تخدم المنصّتين.
5. **لا تفكيكَ للحزم الخمس** — كلُّها تقرأ ملفّ المعلّمين النشط (+ CMS للأخبار) وصفرُ قراءةٍ من `student_file_id`/`schedule_file_id`؛ والتفكيكُ يضاعف الطوابير.
6. **لا كاشَ حافّةٍ للدوالّ الشخصيّة** (بتوكن/`studentId`) — تسريبٌ بين المستخدمين. ولا كاشَ لـ`getStudentsProtected` (تحمل درجاتٍ؛ البائتُ يُكتب فوقه).
7. **النبضةُ تتزامن مع إجهاض الحفظ**: 46% مقابل 7% بالصدفة (الخميس 09-17) — تزامنٌ لا سببيّة.
8. **المشاهداتُ والإعجاباتُ والإحصائيات ~2.5% من الحصّة** — ليست السبب؛ وفحصُ طلب التقييم كان 4% (عولج).
9. **المراكز**: 59% من الحركة عبر مرسيليا (اليمن)؛ ميونخ/باريس اختباراتُ هذا الجهاز (نفقُ فرنسا). الاختلافُ الوحيد بين المراكز **الكاشُ المحلّيّ لكلٍّ منها**، والبياناتُ في مكانٍ واحد.
10. **`_inflateViews`** (عرضٌ للمعلّمين غيرِ المدير): عمليةٌ حسابيّةٌ في المتصفّح — صفرُ نداءٍ وصفرُ كتابة ⇒ لا أخطاءَ ولا استنزاف. قرارُ المالك 09-19: **تقليلُه إلى النصف** — يُنفَّذ في `SchoolApp-gas` (انظر §٥).

## ٤) فخاخُ قياسٍ وقعت في هذه الجلسة — لا تُكرَّر

- `gcloud` **`textPayload:"BOOT-COST"` ⇒ صفرٌ كاذب**؛ الرسالةُ في `jsonPayload.message`.
- **تأخّرُ ظهور سجلّ GAS دقائق** — قلتُ «اختفى السطر» وكان تأخيراً.
- أداةُ observability: **سلسلةٌ بلا `groupBy` لا أعدادَ فيها**، و**عرضُ `events` معطوب** ⇒ الأحداثُ الخام عبر `cloudflare-api` (`/workers/observability/telemetry/query`).
- **طلبُ اختبارٍ بلا `schoolId` في المغلّف ⇒ `nokey·sid`** — يبدو «الكاش لا يعمل» وهو خطأُ المِجَسّ.
- **الجمعة والسبت عطلة** — الأحجامُ لا تُقارَن بيوم دراسة؛ النِّسَبُ وحدَها.

## ٥) ما بقي — بمالكه

| البند | المالك | الحالة |
|---|---|---|
| ✅ تقليلُ `_inflateViews` إلى النصف | `SchoolApp-gas` #1629 ⇒ `teacher@948` (14:22:07Z) + نقلُ الواجهة في PR هذه الوثيقة | **أُنجز**: `real + Math.round(real*2.5) + 15` = نصفُ `real*7+30` بالضبط (مُتحقَّق على 0 · 10 · 40 · 100)، والمديرُ على الحقيقيّ |
| قياسُ أثر اليوم على يوم دراسة (الأحد 09-20 05–11Z مقابل 09-13) | هذه الجلسة/خليفتُها | `pending.json` · `worker-load-relief-after-measure` |
| `upstream_html` بعد حفظٍ ناجح (12:42:32Z) | `SchoolApp-gas` | يُراقَب إن تكرّر |
| CI الخلفية (دقائق Actions) | المالك | كلُّ فرقِ واجهةٍ يدويٌّ حتى يُحلّ |
| هجرةُ Supabase — العلاجُ البنيويّ لزمن القبول | برنامجٌ معتمد | `SchoolApp-gas/DATABASE_MIGRATION_PLAN.md` |

📄 الوثيقةُ السابقة: `_docs/2026-09-18-قراءة-الذروة-وخط-أساس-الأحد.md`.
📄 **تسليمُ الجلسة الشقيقة:** `SchoolApp-gas/_planning/handoff-2026-09-19-أ-حزم-الإقلاع-والقياس-المشترك.md`
(‏بنودُها في `pending.json`: `measure-boot-and-save-after-0919` · `upstream-html-after-successful-save` ·
`diagnose-student-news-hardcoded-id`).
