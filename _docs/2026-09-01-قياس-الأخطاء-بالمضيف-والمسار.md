# أين تقع أخطاء `/gas/*` فعلاً — المضيفُ ليس السبب، والمسارُ هو

**النافذة:** ١٢ ساعة حتى `2026-08-31T23:3xZ` · **المصدر:** `workers/observability/telemetry/query`
عبر `cloudflare-api` · **الوركر:** `school-teacher-proxy`.

> 🔴 **لماذا هذه الوثيقة متتبَّعة:** سجلّاتُ Workers Observability **تنقضي بانقضاء
> الاحتفاظ**، فلا يُعاد إنتاج هذا الجدول بأيّ أمرٍ بعدها. وقاعدةُ الجهاز: ما لا يُعاد
> إنتاجُه بتشغيل أمرٍ يُتتبَّع في المستودع.

---

## ١) الدافع — دعوى كانت ستقود إلى الرافعة الخطأ

وصلت من جلسةٍ نظيرة قراءةٌ صحيحةُ الأرقام: **٣٣٥ من ٣٦٧ خطأً على `school.procorners.com`**
— المضيفِ الإرثيّ — ومنها استُنتجت رافعةٌ محتملة **على الحافّة** (توجيهُ المضيف الإرثيّ أو
تحديدُ معدّله). وأعلنت الجلسةُ بأمانةٍ أنها **لم تقِس مصدرَ حركته ولا نسبتَه من الكلّ**.

🔴 **وذلك المقامُ الغائب هو كلُّ شيء:** نسبةُ الأخطاء بالمضيف بلا نسبةِ **الحركة** بالمضيف
تُنتج «اكتشافاً» لا يقول شيئاً — فالمضيفُ الذي يحمل معظمَ الحركة يحمل معظمَ الأخطاء
**بحكم الحساب لا بحكم العلّة**.

---

## ٢) القياس — بالمضيف أولاً

| المضيف | الردود | 502 | 503 | نسبةُ 5xx |
|---|---:|---:|---:|---:|
| `school.procorners.com` (إرثيّ) | **6,336** | 335 | 130 | **7.34٪** |
| `yemenschoolz.com` (الرسميّ) | 764 | 31 | 0 | **4.06٪** |
| `school-teacher-proxy…workers.dev` | 526 | 0 | 0 | 0٪ |
| `www.yemenschoolz.com` | 45 | 0 | 0 | 0٪ |

🟢 **وضابطُ الاكتمال — وهو شرطُ الاعتماد على أيّ جدول هنا:** مجموعُ الردود
`6336+764+526+45 = 7,671`، والإجماليُّ غيرُ المصفَّر `12,682 − 5,011 = 7,671` ⇒ **تطابقٌ
تامّ، صفرُ ذيلٍ مقطوع.**

⚠️ **و`5,011` صفَّ حالتُه `0` ليست ردوداً** بل أسطرُ لوغ بلا حدثِ طلب (لذلك لا تحمل
`headers.host`) ⇒ تُستبعَد، وإلّا انخفضت كلُّ النسب بالثلث.

🔴 **وفخُّ تجميعٍ وقع فعلاً وأُبطل نتيجتَه:** التجميعُ بمفتاحين معاً
(`host` × `status`) أعاد **١٠ مجموعاتٍ فقط رغم `limit: 400`** ⇒ جدولٌ يبدو مكتملاً
ومجموعُه **1,154** بدل 7,671. **أُسقط كلّياً**، واستُبدل باستعلاماتٍ مُصفّاةٍ لكلّ مضيفٍ
على حدة. (وهي فئةُ الفخّ الموثَّقة سلفاً: **مجموعُ المجموعات يُطابَق بالإجماليّ قبل
الاعتماد**.)

⇒ **الحركةُ نفسُها مركَّزةٌ على الإرثيّ: 82.6٪ من الردود.** فحملُه **91.5٪** من الـ502
**زيادةٌ طفيفة لا اكتشاف**.

---

## ٣) والمسارُ يقلب الاستنتاج — القياسُ الحاسم

| المضيف | المسار | الردود | 502 | 503 | نسبةُ الفشل |
|---|---|---:|---:|---:|---:|
| `school.procorners.com` | **`/gas/student`** | **2,834** | 288 | 130 | 🔴 **14.7٪** |
| `school.procorners.com` | `/gas/teacher` | 902 | 47 | 0 | 5.2٪ |
| `yemenschoolz.com` | `/gas/student` | **30** | 0 | 0 | 0٪ |
| `yemenschoolz.com` | `/gas/teacher` | 333 | 31 | 0 | **9.3٪** |
| `…workers.dev` | `/gas/*` | **2** | 0 | 0 | — |

**ثلاثةٌ تُقرأ من هذا الجدول، وكلُّها تنقض «المضيفُ هو السبب»:**

1. 🔴 **الإرثيّ يحمل ٢٬٨٣٤ من ٢٬٨٦٤ نداءَ طالب — ٩٩٪.** فتركُّزُ الأخطاء عليه **أثرُ مزيج
   الحركة لا خاصّيةٌ فيه**.
2. 🔴 **وعلى المسار نفسِه ينقلب الترتيب:** `/gas/teacher` يفشل **٩٫٣٪ على الرسميّ** مقابل
   **٥٫٢٪ على الإرثيّ** ⇒ **الرسميُّ أسوأ حين يُقارَن بعدلٍ.**
3. 🔴 **وصفرُ `workers.dev` لا يعني صحّة:** حصّتُه من `/gas/*` **نداءان اثنان**؛ و٥٢٦ ردّاً
   الباقية ليست نداءات API. ⇒ **ضابطٌ اختبر المسارَ الذي لا ينكسر فيه** — نفسُ فئةِ الفخّ
   التي ضربتنا ثلاثَ مرّاتٍ في نفس الليلة.

---

## ٤) الأثرُ على القرار

🔴 **رافعةُ الحافّة على المضيف الإرثيّ لا تُقلّل الأخطاء — تنقل الحملَ نفسَه.**
الخانقُ **نداءُ الطالب**: `GAS.student = GAS.teacher` ⇒ مشروعٌ تنفيذيّ واحد، والحصّةُ
‏٣٠ تنفيذاً متزامناً **لكلّ حساب**. والرافعةُ في **المصدر** (عددُ تنفيذات GAS) لا في الوسيط
— وهو ما يقوله بندُ `student-boot-bundle-trim` أصلاً.

### 🔴 ولماذا ٩٩٪ من حركة الطالب على الإرثيّ — لم تعد فرضية، قِيست

**الدليلُ من الحزمة المنشورة نفسِها، لا من الكود وحده** — وهذا ما يجعله قاطعاً:

| | `school.procorners.com` | `yemenschoolz.com` |
|---|---|---|
| **`SchoolApp-v2.8-vc31-2026-07-17.aab`** (المؤرشَفة) | `base/dex/classes.dex` | 🔴 **لا شيء** |
| `vc33` | `base/dex/classes.dex` | `dex` + `AndroidManifest` |

ويوافقه **آخرُ كوميتٍ قبل البناء** `1bf02ae` (‏2026-07-17 01:55 · والنشر 07:40): الروابطُ
الخمسة على `school.procorners.com` نصّاً، **ولا وجودَ لـ`CANONICAL_ORIGIN` أصلاً** في تلك النسخة.
⇒ **قاعدةُ المستخدمين المثبَّتة (‏٦٦٣ تثبيتاً على `vc31`) تشير إلى الإرثيّ حتماً.**

🔴 **ومصيدةُ إسنادٍ كادت تُنتج جواباً صحيحاً بدليلٍ خاطئ — وقعتُ فيها ونقضتها جلسةُ
مالك المستودع:** كُتب هنا أوّلاً أن الدليل هو `08c1d27` «حالةُ `vc31` المنشورة» **نقلاً عن
نظير**. وهو كوميتُ **مصالحةٍ كُتب 2026-08-11 — بعد النشر بأربعة أسابيع**؛ والمستودعُ وقتَ
البناء كان يقول `30`/`"2.7"`. ⇒ **`git show <الكوميت الذي يحمل رقم الإصدار>` يعطي شجرةً
لم تُبنَ منها الحزمةُ قطّ.** و`git tag -l` **فارغٌ تماماً** — «لا وسم» نتيجةٌ لا فشل، ولا
تُستبدَل بتخمين.
⚠️ **وحدُّ قياسِ الحزمة يُقال:** حضورٌ/غياب لا تكرار (‏`grep -c` على `dex` يعدّ سطراً
واحداً طويلاً) — **والغيابُ وحده قاطع**، وهو المطلوب هنا.

🔴 **وأثرُه على القرار قاطع: لا تُطبَّق رافعةُ حافّةٍ على `school.procorners.com`** — أيُّ
تحديدِ معدّلٍ أو حجبٍ أو توجيهٍ عليه **يضرب المستخدمين المثبَّتين أنفسَهم**، لا زائراً
عابراً ولا بوتاً.
⚠️ **وتبعٌ ثانٍ يُكتب سلفاً — ومُثبَتٌ من الحزمة لا مستنتَجاً:** `vc33` **يُبقي**
`school.procorners.com` في الـ`dex` (‏مسارُ ثقةٍ احتياطيّ) **ويضيف** `yemenschoolz.com`.
⇒ نقلُ الأصل **لا يُخفّف الحمل حتى يُحدِّث المستخدمون فعلاً**: أثرٌ **تدريجيٌّ لا لحظيّ**،
وأيُّ توقّعٍ يقول غيرَ ذلك خاطئ. **و`vc33` لم تُرفَع أصلاً بعد.**

### 🔴 والرافعةُ أضيقُ ممّا ظنّ أيٌّ منّا — قياسان يلتقيان

- **هذا القياس:** الرافعةُ في **المصدر** (عددُ تنفيذات GAS) لا في الوسيط.
- **وقياسُ جلسة `SchoolApp-gas`:** بندُ `student-boot-bundle-trim` **أُغلق** — الباقي في
  `getStudentBootBundle` تافه (‏`التعميمات` ٢٦٤ خليّة · `القوائم` ~١٩٢ **ومُكاشة**)
  ⇒ **٠٫٧٪** ممّا عالجه `#1380`.

⇒ **الاثنان صحيحان معاً: الرافعةُ في المصدر — والتقليمُ استُنفد.** فلم يبقَ إلا:
① **تقليلُ عددِ النداءات من جهة العميل**، أو
② 🔴 **فصلُ نشرة الطالب في مشروع GAS مستقلّ** — وهي **الوحيدةُ البنيوية**، لأنها ترفع
   السقفَ نفسَه: اليوم `GAS.student = GAS.teacher` ⇒ مشروعٌ تنفيذيّ واحد يبتلع
   ‏٣٠ تنفيذاً متزامناً لحسابٍ واحد، **فحملُ الطالب يُسقط دخولَ المعلّم**.
   🟢 **وشقُّ الوركر منها سطرٌ واحد** (‏إعادةُ `GAS.student` إلى معرّف نشرته الخاصّة —
   وهو **قائمٌ اليوم كمسارِ تراجعٍ خامل**)، **والحاجزُ كلُّه في جانب GAS وقرارِ المالك.**
   البند: `split-student-gas-deployment`.

---

## ٥) طريقةُ إعادة القياس

🔴 **تصحيحُ أداةٍ مقيسٌ 2026-09-05 — الطريقان انقلبا، وهذا القسمُ كان يقود إلى الطريق المعطوب:**
`cloudflare-api` → `POST /accounts/{id}/workers/observability/telemetry/query` **يردّ 400
اليوم** بالصيغة المكتوبة هنا، بينما **`cloudflare-observability` MCP يعمل** (وكان موصوفاً
«غيرَ مُصادَق»). ⇒ **جرّب الـMCP أوّلاً، وإن ردّ خطأً فجرّب `cloudflare-api`** — وحالةُ
المصادقة تتبدّل بلا إعلان، **فلا يُقدَّم طريقٌ على الآخر بحكمٍ مكتوب.**
📖 `_docs/2026-09-05-قياس-نطاق-الحصة-تجربة-ضابطة.md` §٢.

**وبقيّةُ المنهج صحيحةٌ على الطريقين معاً ولم تتغيّر:**
`datasets: ["cloudflare-workers"]` · مُصفّىً بـ`$metadata.service = school-teacher-proxy`.
🔴 **استعلامٌ مُصفّىً لكلّ مضيف/مسار على حدة**، والتجميعُ بمفتاحٍ **واحد** فقط
(`$workers.event.response.status`, `type: "number"`).
⚠️ و`$workers.event.request.path` **لا يتواجد مع الحالة** — استعمل `…request.url` مع
`operation: "includes"`، و`$workers.event.request.headers.host` للمضيف.
🟢 **ولا يُعتمَد جدولٌ قبل مطابقةِ مجموعه بالإجماليّ غيرِ المصفَّر.**
⚠️ **و`granularity` يُهمَل عملياً في الـMCP** — طلبُ ٣٦٠٠ث أعاد دِلاءَ ~٣٫٥ث و٢٤٬٢٧٤ سطراً
بقيمٍ فارغة. ⇒ **نوافذُ مطلقةٌ صغيرةٌ متعدّدة**، لا دلوٌ دقيقٌ على نافذةٍ واسعة.
