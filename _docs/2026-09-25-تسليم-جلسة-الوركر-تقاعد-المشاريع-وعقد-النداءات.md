# تسليم جلسة الوركر — تقاعدُ المشاريع الأربعة · طرفٌ واحدٌ للبيانات · عقدُ النداءات

> الجلسة: `5339530b` (وركر/ويب) · بالتنسيق مع جلسة الخلفية `final-cleanup-teacher-data`.
> الفترة: 2026-09-24 → 2026-09-25 (UTC). **مالكُ البنود المفتوحة `~/.claude/health/pending.json`** —
> هذه الوثيقة تشير ولا تعيد السرد.

---

## ① ما دخل `main` ونُشر — وتحقّقُه الحيّ

| PR | المستودع | المحتوى |
|---|---|---|
| **#387** | web | تصحيحُ وقائعَ بائتة · مرآةُ بوّابة رمز الطالب `_STU_GATE_MIRROR` (**تقيس ولا ترفض**: `ev:'stugate' act:'would_reject'`) |
| **#1740** | gas | المولِّد يكتب `/gas/home` لصفحة `home-all-school` · لا يولّد `home-all-school/newsarticle.html` · **يفرّغ `frontend/` قبل كلّ بناء** · `check-deployments` مقلوب |
| **#388** | web | حذفُ معرّفات المشاريع الأربعة المتقاعدة من `var GAS` ⇒ **أربعُ وجهاتٍ حيّة** · `GAS.student = GAS.teacher` و`GAS['home-all-school'] = GAS.home` (دائمان) · `/gas/schedule` و`/gas/pricing` ⇒ **410** · `/home-all-school/newsarticle.html` ⇒ **301** إلى `/home/newsarticle.html` مع حفظ الاستعلام |
| **#389** | web | بناءٌ محلّيٌّ للواجهة (بديلُ CI أثناء نفاد Actions): حذفُ `newsarticle` المكرَّرة · `home-all-school/index.html` = `home/schools.html` حرفياً (فرقٌ صفريّ) · بطاقةُ هويّة المعلّمين (gas#1739) |
| **#1742 · #1744 · #1745** | gas | 1أ (نداءات الطالب ركّاباً) · تصحيحاتُ الوثيقة المشتركة و`check-deployments` بالسلالة · تسليمُ الخلفية |

**ترتيبُ الدمج الذي اتُّبع — وعلّتُه:** gas ⇒ **#388 (الوركر) ⇒ #389 (Pages)**. حذفُ صفحةٍ من Pages
يعتمد على تحويلٍ في الوركر ⇒ **الطرفُ المُستهلَك أوّلاً**، وإلّا صار رابطُ الخبر القديم 404 بينهما.

**التحقّقُ الحيّ** (‏`curl.exe -s -D - -o /dev/null` على `school.procorners.com` و`yemenschoolz.com`
و`app.yemenschoolz.com`): المساراتُ المجمَّدة `/home-all-school/` `/student/` `/schedule/` `/teacher/` ⇒ 200 ·
`/gas/schedule` `/gas/pricing` ⇒ 410 · `/pricing` ⇒ 301 `/#pricing` · `newsarticle?news=` ⇒ 301 بالاستعلام ·
أصلُ Pages للملفّ المحذوف ⇒ 404 (الوركر يسبقه) · `health` عبر الاسمين المستعارين ⇒ `ok:true`.
**وبعد نقل المالك المشاريعَ الأربعة إلى سلّة Drive** أُعيد القياس ⇒ لا تغيير.
ونشرت الخلفيةُ `teacher@995 · home@152 · master-admin@192` نحو **22:55–23:01Z 2026-09-24**.

## ② قراراتُ المالك في هذه الجلسة

1. **حذفٌ نهائيّ** للمشاريع الأربعة (`student` · `home-all-school` · `schedule` · `pricing`) ⇒ سلّةُ Drive في اليوم نفسِه، بعد التحقّق من صفر مشغّلات لها (قائمةُ مشغّلات الحساب: 11، كلُّها لمشاريعَ حيّة).
2. **`newsarticle` المكرَّرة ⇒ 301** لا 404.
3. **عقدُ النداءات بمراحل** (M0–M5) لا إعادةُ كتابةٍ شاملة ولا بدءُ هجرة — التفصيل في الوثيقة المشتركة.
4. **قراءةٌ مفتوحة + ملفٌّ مشترك** بين الجلستين، و**جلسةُ الخلفية وحدَها تكفي** بعد نقل حواجز الويب إليها (§④).
5. 🔴 **لا تجميدَ للدمج إطلاقاً** — القياسُ يُقطَع عند طوابع النشر بدل منع الدمج (`CLAUDE.md` §«الدمجُ في أيّ وقت»).

## ③ القياسُ الأساسيّ — مدخلُ M0 (لا يُعاد إنتاجُه بعد انقضاء الاحتفاظ)

النافذة: 7 أيام حتى **2026-09-24 22:30Z** · `ev='gas'` · **السجلُّ عيّنةٌ ~10%** ⇒ الأعدادُ تقديرية
(تتذبذب ~10% بين استعلامين) والنِّسَبُ أمتن. `ms`/`srv` وسيطان بالملّي‑ثانية.
الإجماليّ: student **7,410** · teacher **4,870** · home **930** · master-admin 310 · cms 50.

**الطالب:**

| الدالّة | n | إخفاق | ms | srv |
|---|---|---|---|---|
| getStudentBootBundle | 1,030 | **42%** | 16,577 | 407 |
| studentHeartbeat | 900 | 24% | 8,618 | 835 |
| getStudentNotificationBadge | 490 | 18% | 10,960 | 617 |
| checkPendingPlatformReviewRequest | 440 | 20% | 5,587 | 508 |
| getHomeScheduleBundle (إخفاقُ كاشٍ فقط) | 430 | 16% | 6,224 | **1,683** |
| getStudentSchoolBrand | 290 | 17% | 4,715 | 196 |
| refreshStudentSiblings | 230 | 22% | 5,924 | 1,266 |

⇒ **2.7 نداءٍ مرافقٍ لكلّ إقلاع** · heartbeat/boot ≈ 0.87 · كاشُ الجدول على الحافّة: hit 147 + swr 11 مقابل store 385 (≈27%).

**المعلّم** (أعلى 9 ≈ 62% من إجماليّه): heartbeat 630 (21%، srv 1,150) · getOnlineNow 360 (للمدير، لوحةٌ مفتوحة) ·
getListsData 250 (24%) · handleTeacherLogin 240 (25%) · getTeacherBootBundle 210 (**43%**) · getUnreadNotesCount 210 (29% — **مكرَّرٌ داخل NotifCounts**) ·
getNotifCountsBundle 160 (**srv 6,475** — ثلاثُ قراءاتٍ باردة أكبرُها `اخبار_مشاهدات` ~36 ألف صفّ) · autoSaveGrades 140 (**srv 5,425** — خريطةُ الصفوف الباردة) · checkAppVersion 130 (من تطبيق الأندرويد لا الصفحة — طبيعيّ).
⇒ **الدوريّاتُ ≈ 6.5 لكلّ إقلاع** — الرافعةُ الأكبر (M2).

**الرئيسية:** getHomePageBundle 634 (إخفاقُ كاش، srv 2,473) · recordPublicNewsViewBatch 74 (srv 3,478 — الورقةُ نفسُها) · listPartnerSchoolsPublic 127 (srv 105).

**والتشخيصُ الجامع:** `ms` ≫ `srv` في كلّ دالّة ⇒ **الزمنُ انتظارُ قبولٍ عند Google** (~5 ثوانٍ قبل
`doPost`، n=658 بقياس الساقين) ⇒ **الرافعةُ عددُ النداءات**؛ والفئةُ ① (srv مرتفع) قلّةٌ مسمّاة أعلاه.

## ④ ما تغيّر في بيئة العمل (غيرُ متتبَّعٍ في git — يُذكر هنا لأنه لا يُرى)

- `SchoolApp/.claude/settings.local.json`: `Read(//c/Users/osama/SchoolApp-gas/**)` مع `deny` لملفّات الأسرار
  (‏`sync_config.json` · `.clasprc.json` · `*.jks` · `keystore.properties` · `service-account-key.json`). والخلفيةُ المقابل.
- **جلسةُ الخلفية تحمل هوكس الويب الخمسة** بمساراتها المطلقة (عبر `~/.claude/hooks/web-scope.js`، ضابطُ الطفرات 10/10)
  ⇒ **تعديلُ الوركر ونشرُه منها محروس.** والنسخةُ واحدةٌ هنا ⇒ تعديلُ هوكٍ يسري على الجلستين.
- **الوثيقةُ المشتركة:** `SchoolApp-gas/_docs/عقد-الوركر-والخلفية.md` — تُقرأ قبل أيّ تغييرٍ يمسّ الطرفين، وتُصحَّح برسالةٍ لمالكها.
- 🔴 **كلُّ ذلك يسري من الجلسة التالية** — لا يُحمَّل إعدادٌ في جلسةٍ قائمة.

## ⑤ التالي — بالترتيب

| # | البند (في `pending.json`) | مَن |
|---|---|---|
| 1 | `measure-1a-student-calls-daytime-0925` — نافذةُ 09-25 05:00–15:00Z مقابل 09-24، **مقطوعةً عند طوابع النشر** | أيُّ جلسة (الخلفيةُ تستطيع الآن) |
| 2 | `stugate-would-reject-count-before-edge-reject` — قبل أيّ رفضٍ على الحافّة | الجلسةُ نفسُها |
| 3 | وثيقةُ M0 ثمّ **M2 المعلّم** بترتيب الخلفية: ① إسقاطُ UnreadNotes المكرَّر ② إيقافُ OnlineNow في التبويب المخفيّ ③ عدّادُ مشاهداتٍ مجمَّع (يخدم M3) ④ نبضةٌ بالفرق بعلَمٍ من العميل | الخلفية |
| 4 | `worker-inflight-coalescing-m4-proposal` — قرارُ نطاقٍ من المالك | المالك |
| 5 | تحديثُ `DATABASE_MIGRATION_PLAN.md` (M5) — **تحديثٌ لا إعادةُ قرار**، والفرقُ يُعرَض قبل التحرير | الخلفية |
| 6 | ثلاثةُ مشغّلات Master_Admin على **الإصدار 190** (checkSubscriptions وأخواه) — تعمل صحيحاً اليوم، لكنها **لا تتبع النشراتِ القادمة** حتى يُعاد ربطُها | المالك (موافقةٌ لحظيّة) |

## ⑥ دروسٌ رُصدت

- **البناءُ الذي لا يفرّغ مخرَجه يُبقي يتيماً ينتظر أن يُنشَر** — لا يظهر في CI (checkout نظيف) ويظهر في البناء المحلّيّ وحده. أُصلح في المولِّد (#1740).
- **ذاكرتان بلا مرجعٍ مشترك تتناقضان صامتتين** — «العمودان لم يُحذفا» عند الخلفية نقضه وصفُ نشرةٍ في لوحة المالك. ⇒ الوثيقةُ المشتركة.
- **بندٌ في `pending.json` بـ`done:true` بلا `closedAt`/`outcome` يحجب كلَّ كتابةٍ في السجلّ لكلّ الجلسات** — وقع الليلة وأُكمل من هنا.
