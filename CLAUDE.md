# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

---

# school-app-yemen-web — منصة مدارس الإبداع والتميز الدولية

> هذا الـrepo (**عام**) يحتوي: `frontend/` + `worker/` + `media/`
> كود GAS المصدري والأدوات موجودة في `C:\Users\osama\SchoolApp-gas` (مستودع **خاص**).
> آخر تحديث: يُقرأ من `git log -1 --format=%cd -- CLAUDE.md` — **لا يُكتب يدوياً هنا**.
> (كان مكتوباً «2026-07-09» بينما الملفّ عُدِّل فعلاً في 08-12 و08-28 و08-29 — أيُّ وسم
>  حالةٍ يدويّ يتقادم بصمت، فئة بند 83-ب.)

---

## Scope & Authorization

STRICT SCOPE RULE: only implement exactly what is authorized. Do not add extra phases or features without explicit approval — ask before expanding scope.

---

## AI Bridge Usage in This Repo

🔴 **تصحيحٌ مقيس 2026-09-10 — كان مكتوباً هنا عكسُه، والفقرةُ كانت تُحيل إلى أدواتٍ ميّتة:**
كان: «‏a local multi-model bridge (Claude Code + OpenAI Codex CLI + OpenClaw) … `codex-bridge`
skill or `/ask-both`». **والمقيسُ في الجذر:** `codex` **غائبٌ من `PATH`** · `/ask-both`
**ميّتٌ لا يُستدعى** · وOpenClaw ‏`infer`/`agent` **معطّلان منذ 2026-07-19**.
🟢 **والعاملُ اليوم اثنان: Claude Code و`opencode`** (‏ومهارةُ `opencode-delegate` مُشتغِلة).
⇒ **لا تُحِل إلى `codex-bridge` ولا `/ask-both` كأنهما متاحان.** والمالكُ الواحدُ لحالة
الأدوات: `~/.claude/CLAUDE.md` §AI Tooling — **يُقرأ لحظةَ الحاجة ولا يُنقَل رقمُه هنا.**

**والقاعدةُ التي تبقى مهما تغيّرت الأداة:** أيُّ مسوَّدةٍ مفوَّضةٍ **تبقى مقيَّدةً بقاعدة
النطاق الصارمة** أعلاه (لا تخطّي موافقةٍ لأن نموذجاً آخر كتبها)، **ولا تلمس `frontend/`
أبداً** — مولَّدةٌ من `SchoolApp-gas` وتُدهَس بأوّل دفعة.

---

## Workflow Modes

PLANNING-ONLY mode: When asked to plan, produce execution prompts and doc/memory updates ONLY. Do NOT write or edit production code until explicitly authorized. This complements the scope rule above — planning sessions output a plan/prompt, not a diff.

---

## ما يُعدَّل هنا وما لا يُعدَّل

| الملف/المجلد | الحكم | السبب |
|---|---|---|
| `worker/school-app-proxy.js` | ✅ عدِّل هنا | الـWorker خاص بهذا الـrepo |
| `wrangler.jsonc` | ✅ عدِّل هنا | إعداد Cloudflare Worker |
| `media/covers/` | ✅ يُكتَب هنا (غير كود) | أغلفة الأخبار النصية المُستضافة علناً (يكتب رابطها CMS عبر `_build/social/` من مستودع gas) |
| `frontend/` | ❌ لا تعدِّل | يُبنى تلقائياً من gas repo عبر CI |

**أي تعديل في GAS → اذهب إلى `C:\Users\osama\SchoolApp-gas`** — والمشاريعُ:
`home` · `teacher` · `cms` · `master-admin` (حيّة) · `home-all-school` (مفطومٌ عملياً) ·
`schedule` (متقاعدٌ كمصدرِ حقيقة) · و🔴 **`student` مفطوم: `/gas/student` يُخدَم من نشرة
`teacher`، والتعديلُ الذي يخصّه يقع في `teacher/`**.

---

## المعمارية (ثلاث طبقات)

**طبقة البيانات:** Google Sheets لكل مدرسة + سجل مركزي `Master_Admin_School`
- ID: `10Zk0vwjrHagydYlU0kyjB6X9uoyVCN6sl5nSet1_c7w`
- أعمدة مفتاحية: school_id(0)، teacher_file_id(4)، cms_file_id(6)، subscription_end(9)، is_active(10).

  🔴 **وعمودان متقاعدان — `student_file_id(5)` و`schedule_file_id(7)`: فارغا القيمة ·
  محجوزا الموضع — ولا يُحذف العمود.** وُحِّدت الصيغةُ 2026-09-11 بقرار المالك، بعد أن كان
  هذا الملفّ يقول «لم يعُد عموداً مفتاحياً» **ويُدرج `student_file_id` مفتاحياً في آنٍ معاً**
  — **ونصفان صحيحان يُقرآن منفردَين أسوأُ من خطإٍ صريح.**

  🔴 **والخطرُ بنيويٌّ لا تحريريّ — ولذلك يُكتب هنا:** «لم يعُد مفتاحياً» **تُقرأ إذناً بحذف
  العمود من الورقة** (وقد سُئل عنها حرفياً). والترتيبُ مشتقٌّ من **مصفوفةٍ مثبَّتةٍ في الكود**
  (`MASTER_SCHOOLS_HEADERS` في `SchoolApp-gas/master-admin/_MasterSchema.js`) **لا من ترويسة
  الشيت**، والسجلُّ الحيُّ يحمل الأعمدةَ على القرص ⇒ **حذفُ عمودٍ يُزحزح كلَّ ما بعده يساراً
  في القراءة والكتابة معاً:**

  | الحقل | موضعُه | ما يُقرأ بعد الحذف |
  |---|---|---|
  | `subscription_end` | ٩ | `is_active` ⇒ **اشتراكٌ من الخانة الخطأ** |
  | `is_active` | ١٠ | `plan_type` ⇒ **مدرسةٌ تبدو معطَّلة** |

  ⇒ **اشتراكاتٌ خاطئةٌ ومدارسُ تُغلَق — بصمتٍ تامٍّ وبلا رسالةِ خطإٍ واحدة.**
  🟢 **والقيمةُ فارغةٌ فعلاً** (أُفرِغت 2026-09-05 مع تقاعد `schedule`، وصفرُ قارئٍ
  لـ`student_file_id` منذ 2026-09-11) — **والفراغُ لا يعني جوازَ الحذف.**
  🔗 والطرفُ المقابلُ مطبَّقٌ في `SchoolApp-gas` بنفس الصيغة؛ يوثّقه هنا
  `worker/test-routes.js` (‏`grep -n "schedule_file_id" worker/test-routes.js`).

**طبقة الخلفية:** 7 مشاريع GAS (ES5 صارم: `var`، دوال عادية، بلا قوالب نصية) + نقطة توجيه ثامنة
- `home · home-all-school · teacher · student · cms · schedule · master-admin`
- 🔴 **و«كاملة» بطَلت لثلاثةٍ منها** — الجدولُ التالي هو الحاكم، ويُقرأ قبل أيّ توجيه.

🔴 **وثلاثةٌ منها لم تعُد «كاملة» — حالةٌ تُقرأ قبل أي توجيه (مُصحَّحة 2026-08-29):**

| المشروع | حالتُه الفعلية اليوم |
|---|---|
| `student` | **مُفطَم** — `/gas/student` يُخدَم من **نشرة `teacher`** بمُميِّز `app=student` (‏`school-app-proxy.js` · `GAS.student → GAS.teacher`). ونشرةُ `student` الخاصّة مسارُ **تراجعٍ خامل** لا سطحٌ عامل ⇒ 🔴 لا يُنشَر عليها |
| `home-all-school` | **مُفطَم عملياً** — الجذرُ صار `/home/schools.html` وslug المدرسة `/home/index.html`. وآخرُ تبعيةٍ حيّة (نداءُ OG) نُقلت إلى `GAS.home` في ف2. 🟢 والنشرةُ تبقى حيّةً خاملة و`/home-all-school/index.html` يردّ 200 (بند 104 — تطبيق SchoolzYemen يشير إليه) |
| `schedule` | متقاعدٌ كمصدرِ حقيقة (الشبكةُ صارت مصدرَ الحقيقة — برنامج «ش») |

⇒ **الحصّةُ المشتركة تُقرأ من هنا:** ‏٣٠ تنفيذاً متزامناً **لكلّ حساب** لا لكلّ مشروع، وكلُّها
Execute-as-Me بنفس الحساب. وطيُّ `student` في نشرة `teacher` يجعل حملَ المنصّتين على **مشروع
Apps Script واحد** ⇒ حملُ الطالب يُسقط دخولَ المعلّم (مقيسٌ 2026-08-26).
- 🗑️ **و`pricing` تقاعد 2026-09-10 بقرار المالك — كان مشروعَ GAS ثامناً يُخدَّم عبر مسار
  `/pricing`.** حُذف **معالجُ المسار** من الوركر، وأُسقطت روابطُه الستّةُ من مصدر HTML، ومجلدُ
  المصدر يُحذف في `SchoolApp-gas`. ⇒ **`/pricing` يردّ ٤٠٤ الآن، مقصوداً.**
  🔴 **وشيئان بقيا عمداً ولا يُقرآن سهواً:** **`GAS.pricing`** في جدول النشرات (‏النشرةُ حيّةٌ
  خاملةٌ مسارَ تراجع، **وحجب `protect-deploy-ids` حذفَه بحقّ**، ويحرسه فحصُ «صفرُ قارئ») ·
  و**`'pricing': 1`** في `_RESERVED_TOP_PATHS` (‏إسقاطُه يجعل المسارَ **مرشَّحَ slug مدرسة**
  فيصير قابلاً للاختطاف). 📄 التفصيل: `_docs/2026-09-10-تقاعد-المشاريع-الأربعة-وازدواج-المستودعات.md`.
- كل مشروع: `doGet` (HTML) + `doPost` عبر `ApiEndpoint.js` (JSON API).
- النشر: Execute as **Me** · Access **Anyone** — **لا تغيير Deployment IDs أبداً**.

**طبقة العرض (هذا الـrepo):**
- `frontend/` — واجهة ثابتة على GitHub Pages، تستخدم `gas-bridge.js` لإعادة تعريف `google.script.run` كـ XHR POST على مسار نسبي `/gas/<app>`.
- `worker/school-app-proxy.js` — Cloudflare Worker يخدم الموقع ويمرّر الـAPI (يحلّ مشكلة حجب github.io في اليمن).
- **PWA:** `sw.js` + IndexedDB (`offline-db.js`) + طابور outbox (`offline-sync.js`).

---

## Cloudflare Worker — مسارات رئيسية

الملف: `worker/school-app-proxy.js` · النشرُ على **خمسة مضيفاتٍ كلُّها Custom Domain على
الوركر نفسِه**: `school.procorners.com` (الأساسيّ — يتفادى حجبَ يمن نت لـ`workers.dev`) ·
`yemenschoolz.com` · `app.yemenschoolz.com` · والقديمُ
`school-teacher-proxy.procorners-shop.workers.dev` **لا يزال حيّاً** (تطبيقُ أندرويد يعتمده).

📖 **المالكُ الكاملُ لهذا القسم — الأرقامُ والتواريخُ والتصحيحاتُ وسردُ الحوادث:**
**`_docs/عقد-الوركر-المسارات-والمنظّم-والكاش.md`** (‏نُقل بحرفه 2026-09-12).
🔴 **ولا يُنسَخ منه سطرٌ إلى هنا** — النسختان تتباعدان بصمت، وهي فئةُ بند 83-ب.

**والأحكامُ التي تُقرأ هنا لأنها تُخالَف عند نقطة القرار — وما عداها هناك:**

1. 🔴 **`procorners.com` زونٌ مشتركٌ مع متجرٍ آخر** ⇒ **أيُّ رأسٍ أو تحويلٍ أو سياسةِ كاشٍ
   بلا `http.host` صريحٍ يصيب المتجر.** **وقع فعلاً وأُوقف قبل الدفع.**
   ⚠️ **وموضعُ هذا السطر هنا عمداً وليس تكراراً:** القاعدةُ الكاملةُ في §🔗 أدناه،
   **والقارئُ يلقى اسمَ النطاق هنا** ⇒ **قيدٌ لا يجاور ما يقيّده لا يصل قارئَه.**
2. 🔴 **`app.yemenschoolz.com` بالسابقة `app.` لا العاري — ولا يُصحَّح ظنّاً أنه بقيّةُ اسم.**
   الحجّةُ ليست في سطح الخدمة (المضيفان متكافئان مقيساً) بل في **قائمة ثقة SSL داخل
   التطبيق**: مطابقُها `host === domain || host.endsWith('.' + domain)` ⇒ **إدراجُ العاري
   يوثّق كلَّ نطاقٍ فرعيٍّ تحته**، ومنها سطوحُ التطبيق الشقيق **المنشور**. التضييقُ مقصود.
3. 🔴 **`CANONICAL_ORIGIN` اسمٌ واحدٌ بقيمتين في مستودعين — لا تُوحَّدا، وليستا تناقضاً:**

| الموضع | القيمة | وظيفتُها |
|---|---|---|
| `worker/school-app-proxy.js` (‏`grep -n CANONICAL_ORIGIN`) | `https://yemenschoolz.com` (**العاري**) | **بناءُ الروابط القانونية** — عنوانُ الأرشفة |
| `YemenSchoolz` (تطبيق أندرويد) | `https://app.yemenschoolz.com` | **المضيفُ الذي يُحمَّل** في الـWebView |

🎯 **والسؤالان مختلفان: «أين يُشار إلى الصفحة؟» غيرُ «من أين تُحمَّل؟»** ⇒ **من رأى
القيمتين فظنّهما خطأً يجب توحيدُه، أفسد إحداهما.** سَمِّ بالوظيفة لا بالاسم.

### المسارات

| المسار | الوظيفة |
|---|---|
| `/gas/<app>` | يمرّر POST/GET إلى GAS `/exec` المقابل (home/home-all-school/teacher/student/cms/schedule/master-admin) — 🔴 **ثمانيةُ مساراتٍ وسبعُ وجهات**: `/gas/student` يذهب إلى نشرة `teacher`. انظر §المعمارية والجدولَ الحاكم |
| `/qr-img?url=...` | Proxy لصور QR من `api.qrserver.com` (fallback عند الحجب) |
| `/qr-download?url=&name=` | تحميل QR كـ attachment |
| `/oauth` | إعادة توجيه OAuth من فيسبوك/إنستغرام → GAS CMS |
| ~~`/pricing`~~ | 🗑️ **حُذف 2026-09-10** — يردّ ٤٠٤ مقصوداً. الاسمُ **يبقى محجوزاً** في `_RESERVED_TOP_PATHS` كي لا يُقرأ slug مدرسةٍ فيُختطَف |
| `/media/drive/<fileId>` | بثّ فيديو Google Drive كـ `video/mp4` مع دعم Range requests (بثّ مباشر بلا تخزين، يتجاوز فحص الفيروسات لملفات Drive الكبيرة) |
| `/drive-upload` | وسيط رفع resumable إلى جلسة Drive (PUT مباشر)، مع تحقّق SSRF مقيَّد بنطاق `*.googleapis.com` فقط |
| `/app` · `/download` | **302** إلى صفحة التطبيق على Play (‏`no-store`، ويمرّر `?ref=` كـ`referrer`) — رابط قصير ثابت يُرسَل في واتساب والإشعارات ويُطبَع، فنقطة تغييره واحدة هنا. الاسمان محجوزان في `_RESERVED_TOP_PATHS` **دفاعياً** (المعالج يسبق حساب الـslug أصلاً)، والرؤوس الأمنية تُكرَّر يدوياً لأن المسار يعود مبكراً |
| `/.well-known/assetlinks.json` | Digital Asset Links — يخدم ميزتين معاً: **App Links** (‏`autoVerify`) و**WebAuthn داخل الـWebView** (‏«الدخول بالبصمة»). 🔴 يُخدَم من الوسيط لا من Pages: مسارٌ يبدأ بنقطة وJekyll يتجاهل ما يبدأ بنقطة/شرطة سفلية بلا `.nojekyll` — كان يمكن أن يختفي بصمت |
| `/*` | يخدم الصفحات من GitHub Pages (`procorners-labs.github.io/school-app-yemen-web`) — يحقن أيضاً وسوم OG لكل خبر عبر `?news=<id>` |

عند إضافة مسار جديد: أضفه قبل قسم «خدمة الموقع الثابت» (ابحث عن العنوان — لا تعتمد رقم سطر ثابت، الملف ينمو).

### المنظّم وإعادةُ المحاولة — الأحكامُ وحدَها

- **الميزانيةُ الكلّية `TOTAL_BUDGET_MS = 24,000ms` وهي السقفُ الحاكم** — ومهلةُ المحاولة
  الواحدة **ما تبقّى منها** (`_gasAttemptPlan`)، **وفشلُ المهلة لا يُعيد المحاولة**
  (`_gasShouldRetry`). ⇒ 🔴 **تغييرُ `GAS_MAX_ATTEMPTS` لا يغيّر هذا السقف إطلاقاً**،
  والاشتقاقُ القائم `TOTAL_BUDGET_MS − GAS_ATTEMPT_MARGIN_MS`.
- **محاولتان لا أربع** (فاصل 700ms · POST فقط).
  🔴 **والعددُ قيدٌ لا يُنقض بلا قياسٍ مضادّ** — رفعُه تضخيمُ حملٍ على حصّةٍ مشبَعة.
  🔴 **وتصحيحٌ مقيس 2026-09-12 — كان مكتوباً هنا «⇒ 503 نظيف لا 502 خام عند الاستنفاد»
  وهو باطلٌ في الحالة المهيمنة:** مسارُ الـ503 (`:1712-1716`) مشروطٌ بردٍّ **غيرِ JSON**،
  ورسالةُ الإجهاض التي نبنيها نحن **JSON صالحة** ⇒ **الإجهاضُ عند الميزانية يُخدَم ٥٠٢**
  (`lastStatus = 502` ثمّ `status: lastStatus`). و٥٠٢ هذه **٩٥٪ من كلّ الإخفاق** مقيساً.
  🟢 **ولا أثرَ له على المستخدم — نُقضت الفرضيّةُ المضادّةُ بالقياس:** `gas-bridge.js:109-110`
  يعامل **٥٠٢/٥٠٤ إشباعاً مثل ٥٠٣/٤٢٩ تماماً** منذ 2026-08-24 (‏`_sat`). ⇒ **الوثيقةُ
  صُحِّحت ورمزُ الحالة لم يُغيَّر** — تغييرُه تعديلٌ بلا أثرٍ يبدو إصلاحاً.
- 🟢 **وتراجعٌ إلى نسخةٍ بائتة عند `abort_budget` وحده** (‏2026-09-12 · `API_STALE_MAX_S = 600`)
  — للقراءات العامّة في `API_CACHE_FNS` فقط، بصفرِ حملٍ إضافيٍّ على GAS.
  🔴 **والطزاجةُ صارت مفروضةً في `_apiCacheFreshness` لا في `caches.match`** لأن التخزينَ
  صار بعمرٍ أطول كي يبقى البائتُ مطابَقاً ⇒ **حذفُ البوّابة يجعل البائتَ يُخدَم طازجاً
  بصمت**. ويُسجَّل بـ`act:'stale'` ويُقرأ بالرأس `X-Api-Stale`، **وسجلُّ `ev:'gas'` يبقى
  `ok:false · why:abort_budget`** كي لا ينخفض عدّادُ الإشباع بلا أن يتحسّن الإشباع.
- **المنظّم (bulkhead): 8 متزامنة داخل العامل · 5 لكلّ تطبيق** · والفائضُ طابورُ FIFO
  ثمّ **503**. و**`BH_SEAT_TTL_MS = 30000`** استردادٌ ذاتيٌّ للمقاعد — `finally` **غير
  مضمون في Workers** عند قطع العميل، وقد تسرّبت فعلاً (`p99(n) = 103` مقابل سقف 8).
- 🔴 **ولا يُرفع `BH_ISO_APP`** — **الميزانيةُ هي الخانقة لا السقف** (حُسم بالقياس
  2026-08-31)، ورفعُه يحوّل **503 رخيصاً** (الجسرُ يتراجع لكاش IndexedDB فوراً) إلى
  **502 باهظاً بعد 24 ثانية** على حصّةٍ مشبَعة.
- **زمنُ الانتظار يُخصَم من الميزانية ولا يُضاف فوقها** (`TOTAL_BUDGET_MS = 24000 - _bhWaited`).
- **`?action=health` مُعفى عمداً** — أداةُ تشخيصٍ تُخبر عن حال GAS لا عن حال المنظّم.
<!-- probe:BULKHEAD_MODE_DEFAULT=on — يقرؤه `worker/test-routes.js` ويطابقه بالكود
     وبـ`wrangler.jsonc`. 🔴 قيمةٌ لا تنسيق: تُغيَّر مع الاثنين معاً أو لا تُغيَّر.
     والنثرُ حولَها حرٌّ — أعِد صياغتَه كما تشاء، ولا تحذف هذا السطر. §١٤ في
     `_docs/قواعد-التنظيف.md`. -->
- **مفتاحُ التحكّم:** `env.BULKHEAD_MODE` ∈ `on` (الافتراضي) · `shadow` (يقيس بلا انتظارٍ
  ولا رفض — للمعايرة) · `off`. **وهو مُضافٌ فعلاً إلى `vars` في `wrangler.jsonc`**
  (‏`grep -n '"vars"' wrangler.jsonc` — 🔴 **ولا يُثبَّت رقمُ سطرٍ هنا: يتقادم صامتاً**).
- 🔴 **حدٌّ معماريّ: نداءاتُ GAS→GAS تخرج إلى `/exec` مباشرةً ولا تمرّ بهذا الوسيط**
  ⇒ **تستهلك الحصّة ولا تظهر في سجلّات الوركر إطلاقاً** ⇒ **أيُّ نسبةِ إخفاقٍ تُقرأ من
  هنا تقيس العَرَض لا المستهلك.** (والقائمةُ المصحّحةُ بالقياس في الملفّ المالك.)
- **وحدٌّ معروف:** الحالةُ على مستوى الـisolate ⇒ تكبح التضخيم ولا تفرض حدّاً عالمياً صلباً.

### كاشُ الحافّة لنداءات GAS العامّة

قائمةٌ بيضاءُ ضيّقةٌ حصراً (`API_CACHE_FNS`): `getHomePageBundle` (ttl 120) ·
`getTeacherSchoolBrand` · `getStudentSchoolBrand` · `getHomeScheduleBundle` (**ttl 1800**
— صريحٌ ومقايضتُه مُقرَّةٌ من المالك: تعديلُ الجدول قد يتأخّر ظهورُه حتى نصف ساعة).
`API_CACHE_TTL_S = 600` · حدُّ الجسم 4096.

- 🔴 **والاعتراضُ يقع قبل حجز المقعد** ⇒ الإصابةُ لا تستهلك من سقف المنظّم ولا من حصّة GAS.
- 🔒 **ولا كاشَ سلبيّاً أبداً:** التخزينُ مشروطٌ بـ`good` **و**`ok(b)` الخاصّ بكلّ دالّة،
  ومقصورٌ على استعلامٍ فارغ (يمنع تسميمَ الاستعلام ويُعفي `?action=health` معاً).
- 🔴 **والمفتاحُ رباعيُّ الأبعاد لا ثلاثيّ:** `_apiCacheKey(origin, app, fn, argsKey)`
  — وكذلك `_brandCacheKey` و`_slugsCacheKey`. ⇒ **مدرستان لا تتشاركان مدخلاً** (مقصود)،
  **ومضيفان لا يتشاركانه أيضاً** (أثرٌ جانبيّ مقيس).
  ⚠️ **ولا يُسقَط `origin` من المفتاح** — الأصلُ يدخل في الحمولة المحقونة، فخلطُ الأصول
  يخدم صفحةً بأصلٍ ليس أصلَها. **الرافعةُ `ttl` أطول أو تقليلُ المضيفات، لا مفتاحٌ أوسع.**

### هويةُ المدرسة خادميّاً — `_brandRewrite`

ثلاثةُ سطوحٍ منذ 2026-09-03: `/home/index.html` · وبوّابتا الدخول حين يُعرَف المستأجر
(المقطعُ الثاني في `/teacher/login/<k>` و`/student/<tab>/<k>`، والمعاملُ الصريح على
`/teacher/index.html` و`/student/index.html` و`/portal`).

- 🔴 **والبوّابةُ الشكلية لا تُرخَ** — slug منشورٌ أو UUID حصراً، **لأن القيمة تصير مفتاحَ
  كاشِ حافّة**. **والجذرُ وصفحةُ المكتبة خطٌّ أحمر** (بندا 68/75).
- **الفارغُ لا يُعرَض أبداً**، والحاويةُ المخفيّة لا تُظهَر إلّا بقيمةٍ حقيقية — يحرسه
  ضابطٌ معاكس. (وقرارُ المالك 2026-09-03 أدخل الهاتفَ والعنوانَ وواتساب بعد إسقاطها عمداً.)
- **وعقدُ الخطّافات:** الوسومُ تعيش في مصدر `SchoolApp-gas` وهذا المستودع يشحن الآلية —
  `[data-brand="name|logo|phone|address|whatsapp"]` · `[data-brand-href="phone|whatsapp"]`
  (‏`tel:` و`wa.me/` **بالأرقام وحدها**) · `[data-brand-host="…"]` يُزيل `hidden` بقيمةٍ
  موجودةٍ فقط. و`window.__SCHOOL_BRAND__` يُحقَن حاملاً المعرّفَ القانونيّ.
  ⚠️ **وإعادةُ تسميةِ أيٍّ من ١٨ خطّافاً في مصدر `SchoolApp-gas` تُحمِّر CI هنا** — وهو المقصود.
- **ومعيارُ التراجع مكتوبٌ سلفاً:** `p95(cpu) > 30ms` على `/teacher/` أو أيُّ `1102`
  ⇒ يُقصَر الحقنُ على الصفحة العامّة.

**🟢 وإعادةُ التحقّق بـ304** على الأسطح الثابتة عبر مُصادِق `Last-Modified` مشتقٍّ من تاريخ
المنبع وطابعِ الهوية معاً (الأحدثُ يحكم).

**Deployment IDs الثابتة في الـWorker (لا تغيّرها):** انظر `var GAS` أعلى `worker/school-app-proxy.js`.
🔴 **والجدولُ وحده لا يكفي — اقرأ السطرَ الذي يليه:** `GAS.student = GAS.teacher;` ⇒ القيمةُ
المكتوبة تحت `student:` **ليست وجهةَ `/gas/student`**، بل مسارُ تراجعٍ خامل. ومن يقرأ الجدولَ
مفرداً يخرج بثمانِ وجهاتٍ مستقلّة وهي **سبع**. (‏`grep -n "GAS.student" worker/school-app-proxy.js`.)

---

## CI وسير العمل

**gas repo → web repo (تلقائي):**
```
push to school-app-yemen-gas/main
  → CI: البوّابة كاملةً (**`npm run gate:full`** — لا `gate`) — والعددُ يُقرأ من سطر `RESULT`
     🔴 **والاسمان ليسا مترادفَين، قِيسا من المصدر 2026-09-08:** في `SchoolApp-gas/package.json`
     ‏`gate` = البناءُ والاختباراتُ والحرّاس · و`gate:docs` = فحوصُ الوثائق ·
     و**`gate:full` = `gate && gate:docs`**. و`build-and-deploy.yml` يشغّل **`gate:full`**.
     ⇒ من يكتب «‏`gate` الكاملة» يصف **أضيقَ** ممّا يجري فعلاً.
     لا يُكتب هنا (كان مكتوباً «24 test» وهو بائدٌ بمراحل — فئة بند 83-ب)
  → rsync frontend/ → school-app-yemen-web/main
  → GitHub Pages يُحدَّث + Worker يُنشر تلقائياً (Cloudflare Workers Builds)
```

**هذا الـrepo (`school-app-yemen-web`) — التحقّق أداتان لا واحدة:**
- `node --check worker/school-app-proxy.js` — صحة syntax الـWorker.
- 🔴 **`node worker/test-routes.js` — وهو الحارس الحقيقي**، وكان غائباً عن هذا الملفّ كلّياً.
  يستخرج منطقَ الوركر نفسه بـ`vm` (‏`_RESERVED_TOP_PATHS` · `_schoolSlugFromPath` ·
  `_DEEP_PORTAL_RE` · `_gasAttemptPlan` · `_gasShouldRetry` · كتلةُ كاش الحافّة …)،
  و**غيابُ أيٍّ منها خروجٌ أحمر لا تخطٍّ صامت**. فحوصٌ سلوكية لا نصّية، ولكلّ فحصٍ
  ضابطُه المعاكس (بلاهما يمرّ الملفّ كلُّه أخضرَ لأنه لم يقِس شيئاً).

**قبل أيّ PR على الوركر — الأمران معاً:**
```bash
node --check worker/school-app-proxy.js && node worker/test-routes.js
```


### 🔴 «صمتُ الدمج» — يُعلَن قبل أيّ نافذةِ قياسٍ للـ502

**الدمجُ في `main` هنا نشرٌ حيٌّ للوركر — ولو كان المدموجُ وثيقةً محضة.** والنشرُ **يُدخل
بدءاً بارداً ويصفّر حالةَ المنظّم داخل الـisolate** ⇒ **أيُّ نافذةِ «قبل/بعد» تعبر دمجاً
تحمل متغيّرين، وقياسُها باطل.**

📖 **المالكُ الكاملُ للبروتوكول وقنواتِ الإثبات الأربع وشواهدِها:**
**`_docs/بروتوكول-صمت-الدمج-وقنوات-إثبات-النشر.md`** — 🔴 **ولا يُنسَخ منه سطرٌ إلى هنا.**

**والأحكامُ الأربعة:**
1. **تُسأل الجلساتُ الحيّة قبل فتح النافذة** (‏`ListAgents` + `SendMessage`)، **و`SchoolApp-gas`
   تُسأل أيضاً** — دمجُها **قد** يُنتج دفعةَ `frontend` آلية هنا ⇒ نشرَ وركر.
   🔴 **و«قد» مقصودة: الدفعُ مشروطٌ بفرقٍ غيرِ فارغ، والحكمُ مخرَجُ `rsync` لا نوعُ الملفّ
   المعدَّل ولا نيّةُ الخطوة.** ⇒ **اسأل النظيرةَ عن مخرَج الدفع لا عن نيّة الدمج.**
2. **أثناء النافذة:** العملُ يتراكم على فروعٍ بـPRs **مفتوحةٍ لا مدموجة** — والفرعُ المدفوع
   يؤمّن العملَ تماماً، **والوثيقةُ لا تفقد شيئاً بالانتظار**.
3. **بعد الإغلاق:** تُعلَن النهايةُ صراحةً، ثمّ يُدمج المتراكم.
4. ⚠️ **وهذا بروتوكولٌ سلوكيٌّ لا حاجزٌ آليّ** — لا هوك يمنع دمجاً أثناء نافذةِ قياس،
   ولا CI يعرف أن نافذةً مفتوحة. **يُقال صراحةً بدل الإيهام بحمايةٍ غير قائمة.**

🔴 **وفخُّ مقامٍ يرافقه: مِجَسُّك جزءٌ من المقام.** عشرون نداءَ `?action=health` = **٩١٪**
من حركةِ ساعةٍ كاملة على `/gas/teacher` (‏2026-09-03). ⇒ **اطرح مِجَسّك من المقام، أو
اقطع النافذة قبل أوّل نداءٍ لك.**

---

## Deployment Workflow

After implementing a Worker change: run `node --check worker/school-app-proxy.js`, open a PR, merge, then live-verify via the Worker health endpoint (Cloudflare Workers Builds auto-deploys on push to `main`) before reporting done. Any GAS-side change must be deployed separately via clasp from `SchoolApp-gas` — merging here never deploys backend logic.

Verification checklist:
- After a live check that doesn't show the expected result, rule out Cloudflare edge cache / browser cache before assuming the deploy failed — fetch fresh (cache-busting query param or hard refresh) before concluding.
- Confirm which side actually needs redeploying: a Worker-only change needs this repo's `main` to build; a GAS backend change needs `SchoolApp-gas` clasp deploy — merging a PR here never triggers that.

---

## Environment & Constraints

Environment notes: this repo is a Cloudflare Worker + generated static frontend (no GAS/ES5 code lives here — see Scope table above). Prefer terminal-only workflows (`wrangler`, `gh`, `node --check`); avoid browser automation unless explicitly requested.

---

## Codebase Conventions

Before editing, confirm you're editing `worker/school-app-proxy.js` (the real source) and not `frontend/` (generated output — overwritten by the next CI run from `SchoolApp-gas`).

---

## قواعد إلزامية

1. **فرع لكل تغيير** — لا دفع مباشر إلى `main`.
2. **لا تعديل `frontend/` يدوياً** — هي ناتج مُولَّد، أي تعديل يُحذف بأول CI تلقائي.
3. **ثبات Deployment IDs** — أي تغيير يكسر `gas-bridge.js` + تطبيق الأندرويد.
4. **Worker syntax فقط من هنا** — منطق الجلسات/البيانات/الأمان في `C:\Users\osama\SchoolApp-gas`.

---

## نقاط الفحص الحية

- **Worker health (النطاق القانوني):** `https://school.procorners.com/gas/teacher?action=health`
- **Worker health (workers.dev، لا يزال حيّاً):** `https://school-teacher-proxy.procorners-shop.workers.dev/gas/teacher?action=health`
- **Worker health (يمن سكولز — 🔴 صار سطحَ خدمةٍ لتطبيق `com.proconrers.schoolzyemen` منذ 2026-09-09، لا نطاقَ هويّةٍ فقط):**
  `https://yemenschoolz.com/gas/teacher?action=health`
- **GitHub Pages:** `https://procorners-labs.github.io/school-app-yemen-web/`
- **CI gas repo:** `https://github.com/procorners-labs/school-app-yemen-gas/actions`

<!-- ci-audit-2026-08-29 -->

### وركفلوهاتُ هذا المستودع — مؤشّرٌ لا سرد

📖 **المالكُ الكامل: `_docs/سياسة-الوركفلوهات-وحماية-الفروع.md`** (‏نُقل بحرفه 2026-09-12)
— علّةُ `pages.yml` وإلغاءُ النشر في منتصفه · `ci.yml` · `claude.yml` · `checkout@v7`.
🔴 **ولا يُنسَخ منه سطرٌ إلى هنا.**

**والأربعةُ التي تُقرأ هنا لأن مخالفتَها تكسر شيئاً حيّاً:**
1. 🔴 **`pages.yml` بـ`cancel-in-progress: false`** — دفعتان متقاربتان كانتا تُلغيان النشرةَ
   الأولى **في منتصفها** فيبقى الموقعُ على نسخةٍ قديمة **وكلُّ شيءٍ يبدو ناجحاً**.
2. 🔴 **و`ci.yml` يستثني `main` من الإلغاء** — إلغاءُ تشغيلة `main` يُلغي التحقّقَ من
   الحالة التي يُخدَم منها الموقع، **والفشلُ هناك صامت**.
3. 🔴 **`claude.yml` صلاحيّاتُه `write` ولا تُعاد إلى `read`** — العطبُ موثَّقٌ رسمياً:
   الإجراءُ يعمل ثمّ **لا ينشر تعليقاً ولا مراجعة، بلا أيّ رسالة خطأ**.
4. 🔴 **ولا حمايةَ فروعٍ على `main` هنا — عمداً:** `main` **يتلقّى دفعاً آلياً مباشراً**
   من CI الـGAS عبر `DEPLOY_PAT`، والحسابُ فردٌ لا فريق ⇒ أيُّ مراجعةٍ إلزاميّةٍ **تكسر
   النشرَ الحيّ**. 🔴 **وتبقى القاعدةُ الأشدّ: لا يُحوَّل هذا المستودعُ خاصّاً أبداً** —
   Pages يُطفأ فوراً على خطّة Free فيموت الموقعُ لكلّ مستخدم.

📖 **والمالك الواحد لحقيقة CI: `~/docs/ci-github.md`** — لا يُعاد سردُها هنا.

---

## 🧹 قبل أيّ حذف — **إجراءٌ لا نصيحة**

📖 **`_docs/قواعد-التنظيف.md`** — قائمةُ تحقّقٍ من تسعة بنود، وكلُّ قاعدةٍ فيها **بشاهدها
المقيس** من حالاتٍ وقعت فعلاً.

**وخمسةٌ منها تُذكر هنا لأنها تُخالَف بحسن نيّة:**
0. 🔴 **«مَن يقرأ هذا نصّاً؟» — يُسأل قبل نقلِ أيّ كتلةٍ من هذا الملفّ أو إعادةِ صياغتها**
   (§١٤). وثيقةٌ قد تكون **مُدخَلاً لحارس**، **والتشديدُ البصريُّ داخل عبارةٍ يقرؤها
   مِجَسٌّ جزءٌ من العقد لا من التنسيق** — وقع فعلاً 2026-09-12. **والمرتسى اليومَ علامةُ
   `<!-- probe:… -->`**: النثرُ حولَها حرٌّ، وحذفُها يحمرّ.
1. 🔴 **الحيُّ هو السطحُ لا المشروع** — `student` و`schedule` **صفرُ ملفٍّ في المصدر**
   **والمساران يردّان `200`** بإعادة توليدٍ من مصدرٍ آخر (`SRC_DIR_FOR`). **والفاصلُ
   سؤالٌ واحدٌ لا اسمُ الشيء: هل يحمله ثابتٌ مجمَّدٌ في حزمةٍ منشورة؟**
2. 🔴 **«صفرُ مطابقة» ليست «صفرَ استعمال»** — `grep -c "GAS.schedule"` ⇒ **٠** والمفتاحُ
   **حيّ** (الوصولُ `GAS[app]` ديناميكيّ) ⇒ **يُسأل «مَن المستهلك؟» لا «أين المرجع؟»**.
3. 🔴 **قوائمُ الحجز لا تُنظَّف** — `'pricing'` في `_RESERVED_TOP_PATHS` بعد حذف معالجه:
   **إسقاطُه يجعل المسارَ مرشَّحَ slug مدرسةٍ فيصير قابلاً للاختطاف.**
4. ⚠️ **و`.claude/worktrees/` يحوي نسخاً ميّتةً تُسمّم `grep`** ⇒ **`--exclude-dir=worktrees`
   في كلّ قياس** حتى تُؤرشَف وتُزال.

🔴 **والسؤالُ الأوّلُ يسبقها كلَّها: «ما الذي يُكسَر إن كان حكمي خاطئاً؟» — يُكتب قبل الفعل.**
**و«لا أستطيع قياسَه» ليست «آمن».**

---

## 🔗 حقائقُ مالكُها الجذر — تُجاور هنا ما تقيّده، **لا لأنها لا تصل**

🎯 **المبدأ:** الأصلُ في `~/CLAUDE.md` **يصل هذه الجلسةَ فعلاً** (الترتيبُ بسلسلة الأسلاف)،
**لكنّ الوصولَ إلى النصّ ليس الوصولَ إلى القارئ عند نقطة القرار** ⇒ **«قيدٌ لا يجاور ما
يقيّده لا يصل قارئَه»**. 🔴 **فالمكرَّرُ هنا إشارةٌ لا مضمون** — والأرقامُ والشواهدُ في
موضعٍ واحدٍ فلا تتباعد نسختان.

📖 **المالكُ الكاملُ للتسعة بشواهدها: `_docs/حقائق-الجذر-المجاورة-تسعة-بنود.md`**
(‏نُقل بحرفه 2026-09-12). 🔴 **ولا يُنسَخ منه رقمٌ ولا حالةٌ إلى هنا** (فئة بند 83-ب).

| # | الحكم — ولا شيءَ سواه هنا |
|---|---|
| **①** | 🔴 **زونُ `procorners.com` مشتركٌ مع متجرِ ركن التسوق** ⇒ **`http.host` صريحٌ في كلّ قاعدة**، وإلّا أصابت المتجر. **ثلاثةُ أوجهٍ للضرر:** HSTS يثبّت ١٨٠ يوماً **بلا رجعة** (المتصفّحُ يحفظ التعليمةَ عند الزائر لا عندك) · و`301` **يفتح المتصفّح ويترك التطبيقَ المنشورَ فارغاً** · وكاشُ زونٍ على `/gas/*` **يقدّم ردَّ مدرسةٍ لأخرى**. ⚠️ **والمشحونُ اليومَ HSTS `max-age=300` مشروطٌ بالمضيف — قائمٌ بحقّ، ولا `preload` أبداً** |
| **②** | 🔴 **المستودعُ عامّ وفي تاريخه مفاتيحُ ما زالت صالحة** — و`secret-scan` مداهُ **كوميتاتُ الدفعة لا التاريخ** ⇒ **حارسٌ أخضرُ يعني «الدفعةُ نظيفة» لا «التاريخُ نظيف»**. والعلاجُ **تدويرُ المفاتيح**؛ وإصلاحُ الكود لا يُبطل ما في التاريخ |
| **③** | 🔴 **«نجح CI» ≠ «نُشر» — سببان لا سبب:** الفرقُ الفارغُ في `frontend/` · **و`workflow_dispatch` يتخطّى خطوةَ الدفع كلَّها والبوّابةُ خضراء** ⇒ من ينفي الأوّلَ بقياسٍ صحيحٍ يستنتج «نُشر» خطأً |
| **④** | 🔴 **تحت `--squash` الكوميتُ لا يدخل `main` ولو دخل محتواه كلُّه** ⇒ **يُقاس المحتوى بـ`git show origin/main:<الملفّ>` لا نسبةُ الكوميت.** ⚠️ **ويُقرأ من `origin/main` لا من الفروع** — الفرعُ البائتُ يُظهر حالةً ميتةً تبدو حيّة |
| **⑤** | ⚠️ **مستودعُ الشقيق يُقاس من كائنات git لا من شجرة عمله** — شجرتُه ليست حالتَه. 🔒 **ولا يُقاس مستودعٌ ليس لجلستك بأمرٍ يكتب فيه** (‏`fetch`/`checkout`) |
| **⑥** | ⚠️ **حمايةُ الفروع ممكنةٌ تقنياً هنا ومرفوضةٌ عمداً** — و**يكفي وقوعُ الدفع الآليّ مرّةً واحدةً لتكسر الحمايةُ النشرَ الحيّ** ⇒ الشرطيّةُ لا تُقرأ تخفيفاً |
| **⑦** | ⚠️ **`measure-guard.js` محلّيٌّ لهذا المستودع وحدَه** — لا يسري على جلسةٍ جذرُها غيرُه، **فلا يُقرأ حمايةً على مستوى الجهاز** |
| **⑧** | ⚠️ **`.github/claude-context.md` ذاكرةُ هذا المستودع لمراجع CI** — `claude[bot]` لا يرى `~/.claude/` ⇒ **أيُّ ثابتٍ حرِجٍ يُحدَّث هنا يُحدَّث هناك أيضاً**، وإلّا راجَع البوتُ على ثابتٍ بائتٍ بلا أن يعرف |
| **⑨** | 📖 **خريطتان مالكُهما الجذر — إحالةٌ لا نقل:** `~/docs/data-map.md` (**تُقرأ قبل أيّ حذف**) · `~/docs/operating-guide.md`. ⚠️ **والجذرُ بائتٌ عن هذا المستودع في مواضعَ مقيسة** ⇒ 🔴 **لا يُنقَل منه رقمٌ ولا حالةٌ بلا إعادةِ قياس** |

🔒 **وخمسُ خطواتٍ مُلزِمةٍ لأيّ تعديلِ زون — إجراءٌ لا تحذير** (التفصيلُ في الملفّ المالك):
1. **استخرج الوجهاتِ من الحزمة المنشورة لا من الكود** — الفرقُ مقيس: `schedule/index.html`
   **في المصدر وغائبٌ عن الحزمة**. ⇒ الكودُ يصف ما نبني، **والحزمةُ تصف ما يعمل عند المستخدم.**
2. **مسبارٌ قبليٌّ على كلّ مسار:** `curl.exe -s -D - -o /dev/null --max-time 60`
   🔴 **ولا راية `-w`** — تعطي `000` كاذباً على هذا الجهاز.
3. **أعِد المسبارَ بعد التطبيق** ⇒ **صفرُ تغيّرٍ في الحالة على كلّ المسارات.**
4. 🔴 **ضابطٌ معاكسٌ على مضيف المتجر** — **قاعدةٌ لا يتغيّر مخرَجُها بين الصحيح والخطأ ليست
   قاعدة**، وهي التي تُنتج «تعديلاً بلا أثرٍ يبدو ناجحاً».
5. **`http.host` صريحٌ في كلّ قاعدة** · **وطابِق المسارَ كما يكتبه العميلُ المجمَّد لا كما
   تفترضه** (‏`/cart` ≠ `/cart/` — وهي بعينها ثغرةُ استثناء الكاش القائمة).
