# دليل التشغيل — العمل دون اتصال والمزامنة التلقائية

هذا الدليل يشرح **بالضبط** ما عليك فعله بعد إضافة ميزة العمل دون اتصال (Offline-First).
الخبر الجيد: الميزة كلها في الواجهة، فلا حاجة لإعادة نشر Google Apps Script ولا Cloudflare.

---

## ١) ما الذي تمّ تلقائياً نيابةً عنك
- دُمجت الميزة في فرع `main`، وGitHub Pages ينشر مجلّد `frontend/` تلقائياً.
- أُضيف فحص آلي (CI) يتحقّق من سلامة البناء والاختبارات عند كل تغيير.
- التطبيق أصبح قابلاً للتثبيت على الجوال (PWA) ويعمل دون اتصال.

## ٢) ما عليك فعله (مرة واحدة فقط)
1. **تأكّد أن GitHub Pages مُفعّل:** في صفحة المستودع على GitHub →
   **Settings → Pages → Source = GitHub Actions**. (غالباً مُفعّل مسبقاً.)
2. **لا شيء على Google Apps Script.** يبقى كما هو.
3. **لا شيء على Cloudflare Worker.** يبقى كما هو — رابطك الحالي
   (مثل `https://school-app.<حسابك>.workers.dev/`) هو رابط التطبيق.

> ملاحظة: أنت لا تحتاج تثبيت أي شيء على جهاز الكمبيوتر. التطبيق موقع ويب يفتحه المستخدمون
> من المتصفّح أو الجوال عبر رابط Cloudflare.

## ٣) كيف يستخدمه المعلّم/الطالب (تشغيل الميزة)
1. افتح رابط التطبيق **وأنت متصل بالإنترنت** أول مرة.
2. **حدّث الصفحة مرة واحدة** (Refresh) — بهذا يُثبَّت Service Worker الذي يخزّن التطبيق.
3. على الجوال: من قائمة المتصفّح اختر **«إضافة إلى الشاشة الرئيسية»** ليصبح كتطبيق مستقل.
4. بعدها يعمل التطبيق حتى لو انقطع الإنترنت.

## ٤) ماذا يحدث عند انقطاع الإنترنت
- **تفتح الصفحات** وتعرض آخر البيانات المحفوظة (كل التطبيقات).
- **المعلّم يستطيع رصد الحضور/الدرجات/تعديل القوائم** — تُحفظ محلياً في طابور آمن.
- تظهر **شارة سفلية** بحالة الاتصال وعدد العمليات بانتظار المزامنة.
- **الجلسة تبقى محفوظة** فيعمل التطبيق حتى لو أُغلق وأُعيد فتحه دون نت
  (شرط تسجيل دخول واحد عبر الإنترنت أول مرة).

## ٥) ماذا يحدث عند عودة الإنترنت
- يُزامِن التطبيق **تلقائياً** كل العمليات المعلّقة بالترتيب، ويظهر إشعار «تمت المزامنة».
- لا تُحذف أي عملية قبل تأكيد نجاحها على الخادم (لا فقدان بيانات).
- إذا رفض الخادم عملية (مثل توكن منتهٍ) تُعلَّم «فاشلة» وتظهر لك لمراجعتها.

## ٦) كيف تختبر أن كل شيء يعمل (خطوة بخطوة)
1. افتح **لوحة المعلّم** متصلاً، وسجّل الدخول، وافتح شاشة الحضور (لتُخزَّن البيانات).
2. افصل الإنترنت (أو فعّل وضع الطيران).
3. غيّر حالة حضور طالب واضغط **حفظ** → ستلاحظ الشارة السفلية وعدّاد العمليات المعلّقة.
4. أعد تشغيل الإنترنت → خلال ثوانٍ يظهر إشعار **«تمت مزامنة N عملية»**.
5. حدّث الصفحة وتأكّد أن الحضور ظهر بشكل صحيح (وفي Google Sheet لديك).

## ٧) عند تحديث الواجهة مستقبلاً (للمطوّر)
- عدّل ملفات المصدر في مجلّدات التطبيقات (مثل `teacher/Teacher Dashboard.html`).
- شغّل: `node _build/build-frontend.js` لإعادة توليد `frontend/`.
- شغّل الاختبارات: `node _build/test-offline.js`.
- ادفع إلى `main` — يتكفّل CI بالتحقّق وGitHub Pages بالنشر.
- عند تغيير منطق التخزين دون اتصال، ارفع رقم نسخة الكاش في `frontend/sw.js`
  (`CACHE = 'creativity-shell-vN'`) ليحصل المستخدمون على النسخة الجديدة.

## ٨) (اختياري لاحقاً) حماية إضافية ضد التكرار على الخادم
للحصول على ضمان أقوى ضد تكرار العمليات على شبكة سيّئة جداً، يمكن إضافة فحص مُعرّف عملية
(`opId`) في ملفات `*/ApiEndpoint.js` (تخزين مُعرّفات آخر 24 ساعة في `CacheService` وتجاهل
المكرّر). هذا يتطلّب إعادة نشر مشاريع GAS الخمسة مرة واحدة، ويُنفَّذ عند الحاجة فقط.
